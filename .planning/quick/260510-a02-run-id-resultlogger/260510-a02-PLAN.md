---
phase: quick-260510-a02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/benchmark/utils/logger.py
  - src/benchmark/cli.py
autonomous: true
requirements:
  - prevent-run-overwrite
must_haves:
  truths:
    - "Each benchmark run writes to results/{model}/{run_id}/ — no overwrite"
    - "run_id auto-generates as YYYYmmdd_HHMMSS when not supplied"
    - "CLI --run-id flag lets user resume or target a specific run"
    - "Global results.csv continues to append all runs (history preserved)"
  artifacts:
    - path: "src/benchmark/utils/logger.py"
      provides: "ResultLogger with run_id parameter and updated path logic"
      contains: "self.run_id"
    - path: "src/benchmark/cli.py"
      provides: "run and merge commands with --run-id option"
      contains: "--run-id"
  key_links:
    - from: "ResultLogger.__init__"
      to: "save_stage_files"
      via: "self.run_id injected into stage path"
      pattern: "self\\.run_id"
    - from: "cli.run_benchmark"
      to: "ResultLogger"
      via: "run_id=run_id or \"\""
      pattern: "run_id="
---

<objective>
Add `run_id` to ResultLogger so each benchmark run writes to its own timestamped
subdirectory under `results/{model}/{run_id}/`, preventing data overwrite between
repeated runs and enabling resume via `--run-id` CLI flag.

Purpose: Scientific rigor — every benchmark run is independently retrievable.
Output: Updated logger.py and cli.py with non-breaking run_id support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<interfaces>
<!-- Current ResultLogger.__init__ signature (from src/benchmark/utils/logger.py:81-89) -->
```python
class ResultLogger:
    def __init__(
        self,
        output_dir: Path = Path("results"),
        hardware: HardwareInfo | None = None,
    ) -> None:
```

<!-- Current save_stage_files path logic (lines 134-148) -->
```python
stage_dir = self.output_dir / result.model_name          # results/{model}
csv_path  = stage_dir / f"{result.stage}.csv"            # results/{model}/{stage}.csv
json_path = stage_dir / f"{result.stage}.json"
```

<!-- Current merge_to_unified path logic (lines 178-184) -->
```python
model_dir  = self.output_dir / model_name                # results/{model}
stage_csvs = sorted(model_dir.glob("*.csv"))
```

<!-- Current CLI ResultLogger instantiation (cli.py:182) -->
```python
result_logger = ResultLogger(output_dir=resolved_dir, hardware=hw)
```

<!-- Current CLI merge instantiation (cli.py:226) -->
```python
result_logger = ResultLogger(output_dir=resolved_dir)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add run_id to ResultLogger and update file paths</name>
  <files>src/benchmark/utils/logger.py</files>
  <behavior>
    - Test 1: ResultLogger() with no run_id auto-generates a non-empty string matching \d{8}_\d{6}
    - Test 2: ResultLogger(run_id="my_run") stores self.run_id == "my_run"
    - Test 3: save_stage_files writes to output_dir/model_name/run_id/stage.csv (not output_dir/model_name/stage.csv)
    - Test 4: merge_to_unified reads from output_dir/model_name/run_id/*.csv
    - Test 5: _append_csv still writes to output_dir/results.csv (global log unchanged)
  </behavior>
  <action>
Edit `src/benchmark/utils/logger.py`:

1. In `__init__`, add `run_id: str = ""` as third parameter (after `hardware`):
   ```python
   def __init__(
       self,
       output_dir: Path = Path("results"),
       hardware: HardwareInfo | None = None,
       run_id: str = "",
   ) -> None:
   ```
   In body, auto-generate if empty:
   ```python
   self.run_id: str = run_id if run_id else datetime.now(tz=UTC).strftime("%Y%m%d_%H%M%S")
   ```

2. In `save_stage_files`, change `stage_dir` to include `self.run_id`:
   ```python
   stage_dir = self.output_dir / result.model_name / self.run_id
   ```
   Update class docstring path examples to show `results/{model_name}/{run_id}/{stage}.csv`.

3. In `merge_to_unified`, change `model_dir` to include `self.run_id`:
   ```python
   model_dir = self.output_dir / model_name / self.run_id
   ```
   Update docstring: reads from `results/{model_name}/{run_id}/*.csv`.

4. `_append_csv` — NO change. Still writes to `self.output_dir / "results.csv"`.

5. Update class-level docstring to describe the new path structure.

Do NOT change `BenchmarkResult` dataclass. Do NOT change `save_json` (it uses
`self.output_dir` directly — unrelated to per-run files).

ruff constraints:
- `from __future__ import annotations` already present — keep it
- All new code must pass `ruff check --select ALL` (except project-ignored codes)
- No inline f-strings in raise statements — use `msg` variable pattern
  </action>
  <verify>
    <automated>cd c:/projects/ResearchSSAU/VKR_Claude && uv run ruff check src/benchmark/utils/logger.py && uv run ruff format --check src/benchmark/utils/logger.py</automated>
  </verify>
  <done>
    - `self.run_id` set to provided value or auto-generated timestamp
    - save_stage_files writes to `{output_dir}/{model}/{run_id}/{stage}.csv`
    - merge_to_unified reads from `{output_dir}/{model}/{run_id}/*.csv`
    - _append_csv unchanged (still writes to `{output_dir}/results.csv`)
    - ruff passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --run-id option to CLI run and merge commands</name>
  <files>src/benchmark/cli.py</files>
  <action>
Edit `src/benchmark/cli.py`:

1. In `run_benchmark` command, add `run_id` parameter before the function body:
   ```python
   run_id: Annotated[
       str | None,
       typer.Option("--run-id", help="Run ID for resuming a specific run (auto-generated if not provided)"),
   ] = None,
   ```
   After `result_logger = ResultLogger(output_dir=resolved_dir, hardware=hw)` line, change to:
   ```python
   result_logger = ResultLogger(output_dir=resolved_dir, hardware=hw, run_id=run_id or "")
   ```
   After creating result_logger, print run_id before the stage loop:
   ```python
   typer.echo(f"Run ID: {result_logger.run_id}")
   ```

2. In `merge_results` command, add `run_id` parameter:
   ```python
   run_id: Annotated[
       str | None,
       typer.Option("--run-id", help="Run ID to merge (required if multiple runs exist)"),
   ] = None,
   ```
   Change instantiation to:
   ```python
   result_logger = ResultLogger(output_dir=resolved_dir, run_id=run_id or "")
   ```

ruff constraints — same as Task 1. No `print()` — use `typer.echo()` only.
  </action>
  <verify>
    <automated>cd c:/projects/ResearchSSAU/VKR_Claude && uv run ruff check src/benchmark/cli.py && uv run ruff format --check src/benchmark/cli.py</automated>
  </verify>
  <done>
    - `benchmark run --run-id my_run` passes run_id to ResultLogger
    - `benchmark run` without --run-id auto-generates run_id, prints it
    - `benchmark merge --run-id my_run` targets correct subdirectory
    - ruff passes with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | run_id value passed from user controls subdirectory path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-a02-01 | Tampering | run_id path component | mitigate | Strip path separators: validate run_id matches `^[\w-]+$` or sanitize with `Path(run_id).name` to prevent directory traversal (e.g. `../../etc`). Apply in `ResultLogger.__init__` when run_id is provided by caller. |
| T-a02-02 | Denial of Service | auto-generated run_id collision | accept | Timestamp granularity is 1 second; consecutive runs within the same second reuse the same directory. Acceptable for academic benchmark use — not a concurrent-write scenario. |
</threat_model>

<verification>
End-to-end check (manual, no GPU required):

```bash
# Verify path structure with a dry import
cd c:/projects/ResearchSSAU/VKR_Claude
uv run python -c "
from benchmark.utils.logger import ResultLogger
from pathlib import Path
import tempfile, os
with tempfile.TemporaryDirectory() as d:
    rl = ResultLogger(output_dir=Path(d))
    print('auto run_id:', rl.run_id)
    assert len(rl.run_id) == 15, f'unexpected length: {rl.run_id}'
    rl2 = ResultLogger(output_dir=Path(d), run_id='test_run')
    assert rl2.run_id == 'test_run'
    print('run_id assignment: OK')
"
```

```bash
# Verify CLI --run-id option is registered
uv run benchmark run --help | grep run-id
uv run benchmark merge --help | grep run-id
```
</verification>

<success_criteria>
- `ResultLogger(run_id="")` auto-generates a 15-char timestamp string (YYYYmmdd_HHMMSS)
- `ResultLogger(run_id="foo")` stores `self.run_id == "foo"`
- `save_stage_files` writes under `{output_dir}/{model}/{run_id}/`
- `merge_to_unified` reads from `{output_dir}/{model}/{run_id}/`
- `_append_csv` still writes to `{output_dir}/results.csv`
- CLI `run` prints Run ID before stage loop
- CLI `run` and `merge` both accept `--run-id` option
- `ruff check` passes on both modified files
</success_criteria>

<output>
After completion, create `.planning/quick/260510-a02-run-id-resultlogger/260510-a02-SUMMARY.md`
</output>
