---
phase: 07-yolo-family-quantization-stages-2-6
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tests/test_tensorrt_engine.py
  - .planning/phases/07-yolo-family-quantization-stages-2-6/07-02-SUMMARY.md
autonomous: false
requirements:
  - OPT-YOLO-02
  - OPT-YOLO-05
user_setup: []

must_haves:
  truths:
    - "A TensorRT TF32 engine builds successfully for YOLO11l and for YOLO26l and produces a Stage 3 BenchmarkResult"
    - "A TensorRT FP16 engine builds successfully for YOLO11l and for YOLO26l and produces a Stage 4 BenchmarkResult"
    - "A TensorRT BF16 engine either builds for both YOLO models or is recorded as gracefully skipped with a skipped_reason"
    - "Stage 3-4 results for both YOLO models appear in results.csv with model_name and stage columns and a non-zero map_50 for the TF32 and FP16 engines"
  artifacts:
    - path: "engines/yolo11l_tf32.engine"
      provides: "YOLO11l TensorRT TF32 engine (built at execution time)"
    - path: "engines/yolo11l_fp16.engine"
      provides: "YOLO11l TensorRT FP16 engine (built at execution time)"
    - path: "engines/yolo26l_tf32.engine"
      provides: "YOLO26l TensorRT TF32 engine (built at execution time)"
    - path: "engines/yolo26l_fp16.engine"
      provides: "YOLO26l TensorRT FP16 engine (built at execution time)"
    - path: "results/results.csv"
      provides: "Stage 3-4 metric rows for yolo11l and yolo26l"
      contains: "3_trt_tf32"
  key_links:
    - from: "src/benchmark/cli.py"
      to: "src/benchmark/engines/tensorrt_engine.py"
      via: "_run_stage constructs TensorRTEngine(precision='tf32'|'fp16'|'bf16') for stages 3_trt_tf32 / 4_trt_fp16 / 4_trt_bf16"
      pattern: "TensorRTEngine"
    - from: "src/benchmark/engines/tensorrt_engine.py"
      to: "engines/{model}_{precision}.engine"
      via: "model-name-keyed engine path from Plan 01"
      pattern: "model_token"
---

<objective>
Verify and lock in TensorRT standard-precision optimization (Stages 3-4) for the YOLO
family: TF32, FP16, and BF16 engines for YOLO11l and YOLO26l.

The CLI orchestration for stages `3_trt_tf32`, `4_trt_fp16`, and `4_trt_bf16` already
exists in `cli.py._run_stage` and already routes through `TensorRTEngine`. Plan 01 fixed
the engine-filename collision so YOLO engines no longer overwrite RT-DETR's. This plan's
job is therefore **verification-driven**: confirm the existing builder produces valid
engines for the YOLO graph topology (which differs from RT-DETR — CNN backbone, YOLO11's
DFL/C2PSA head, YOLO26's NMS-free `(1,300,6)` output), confirm BF16 is gated correctly
on the Ampere RTX 3070, and confirm per-stage metrics land in the unified results files.

No net-new engine code is expected (D-04: reuse the v1.0 builder unchanged). If the
existing builder fails on a YOLO graph, that is a real finding to surface — but the
default expectation per RESEARCH.md is that the architecture-agnostic builder handles it.

Purpose: Stages 3-4 are the standard-precision baseline that Stage 5 (INT8) and Stage 6
(Mixed Precision) are measured against.
Output: Built TF32/FP16/BF16 engines for both YOLO models, Stage 3-4 rows in
results.csv/results.json, a strengthened TRT engine unit test, and a checkpoint
confirming the engines run correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-yolo-family-quantization-stages-2-6/07-CONTEXT.md
@.planning/phases/07-yolo-family-quantization-stages-2-6/07-RESEARCH.md
@.planning/phases/07-yolo-family-quantization-stages-2-6/07-VALIDATION.md
@.planning/phases/07-yolo-family-quantization-stages-2-6/07-01-SUMMARY.md

<interfaces>
<!-- Existing contracts. Extracted from src/. -->

From src/benchmark/cli.py — the stage-3/4 branch already exists:
  elif stage in ("3_trt_tf32", "4_trt_fp16", "4_trt_bf16"):
      precision_map = {"3_trt_tf32": "tf32", "4_trt_fp16": "fp16", "4_trt_bf16": "bf16"}
      engine = TensorRTEngine(model_name=..., precision=..., engine_dir=..., adapter=..., force_rebuild=...)
      engine.load_model(onnx_path)
      result = engine.run_full_benchmark(dataloader, stage=stage, baseline_map_50_95=..., macs=..., flops=...)

From src/benchmark/engines/tensorrt_engine.py:
  - _build_engine sets config.set_memory_pool_limit(WORKSPACE, 2 << 30)  # D-06: strict 2 GB
  - tf32  -> config.set_flag(trt.BuilderFlag.TF32)
  - fp16  -> config.set_flag(trt.BuilderFlag.FP16)
  - bf16  -> if not builder.platform_has_tf32: raise _BF16UnsupportedError; else config.set_flag(trt.BuilderFlag.BF16)
  - BF16 build failure is caught in load_model() -> sets self._skipped_reason, run_full_benchmark returns a NaN BenchmarkResult with skipped_reason populated
  - optimization profile fixes batch=1 (min=opt=max) from the dynamic ONNX axis

From src/benchmark/models/yolo_adapter.py:
  YOLOAdapter(is_nms_free=...).parse_outputs(raw_outputs, original_size, input_size, score_threshold)
  # YOLO11l: is_nms_free=False, raw output (1,84,8400), NMS in post-processing (D-03)
  # YOLO26l: is_nms_free=True,  raw output (1,300,6)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a YOLO-graph TensorRT build test asserting the 2 GB workspace and precision flags</name>
  <files>tests/test_tensorrt_engine.py</files>
  <read_first>
    - tests/test_tensorrt_engine.py (the file being modified — note it mocks `tensorrt` at module level via `sys.modules["tensorrt"] = trt_mock` and uses `patch("benchmark.engines.tensorrt_engine.trt")` for build tests; Plan 01 already updated the two existing tests to model-scoped filenames)
    - src/benchmark/engines/tensorrt_engine.py (`_build_engine` — the precision-flag branches and `config.set_memory_pool_limit(trt.MemoryPoolType.WORKSPACE, 2 << 30)` call; the bf16 `platform_has_tf32` gate)
    - .planning/phases/07-yolo-family-quantization-stages-2-6/07-CONTEXT.md (D-04, D-05, D-06 — reuse the builder, TF32/BF16 flags, strict 2 GB workspace)
  </read_first>
  <action>
    Add a parametrized unit test to tests/test_tensorrt_engine.py that constructs a `TensorRTEngine` with `model_name="yolo11l"` for each of precision `"tf32"` and `"fp16"`, runs `_build_engine` against a dummy onnx path with `trt` patched (follow the existing `test_tensorrt_engine_build_mixed_strategy` mock pattern: patch `benchmark.engines.tensorrt_engine.trt`, set `mock_builder.build_serialized_network.return_value = b"serialized_data"`, write a dummy `.onnx` file), and asserts: (a) `config.set_memory_pool_limit` was called with `mock_trt.MemoryPoolType.WORKSPACE` and `2 << 30` (D-06 — strict 2 GB); (b) for precision "tf32", `config.set_flag` was called with `mock_trt.BuilderFlag.TF32`; for "fp16", with `mock_trt.BuilderFlag.FP16` (D-05). Add a separate test for BF16: with `model_name="yolo26l"`, precision `"bf16"`, set `mock_builder.platform_has_tf32 = True` and assert `config.set_flag` was called with `mock_trt.BuilderFlag.BF16`; then a second case with `mock_builder.platform_has_tf32 = False` asserting `_build_engine` raises the module's `_BF16UnsupportedError` (import it from the module). Do not modify any source file — this task is test-only; it pins the contract that Plan 01's filename change did not disturb the build path and that the YOLO model_name flows through correctly.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tensorrt_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tensorrt_engine.py contains a test that asserts `set_memory_pool_limit` is called with `2 << 30` for a `model_name="yolo11l"` engine
    - tests/test_tensorrt_engine.py asserts `BuilderFlag.TF32` is set for precision "tf32" and `BuilderFlag.FP16` for precision "fp16"
    - tests/test_tensorrt_engine.py asserts BF16 build raises `_BF16UnsupportedError` when `platform_has_tf32` is False and sets `BuilderFlag.BF16` when True
    - `uv run pytest tests/test_tensorrt_engine.py` exits 0
    - `uv run ruff check tests/test_tensorrt_engine.py` exits 0
  </acceptance_criteria>
  <done>The TRT engine test suite pins the YOLO-model build contract: 2 GB workspace, correct TF32/FP16/BF16 flags, and the Ampere BF16 gate — all with model_name="yolo11l"/"yolo26l".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Build and benchmark Stages 3-4 for YOLO11l and YOLO26l on the RTX 3070</name>
  <what-built>
    Plan 01 added the YOLO ONNX export and fixed the engine-filename collision. The CLI
    already orchestrates stages 3_trt_tf32, 4_trt_fp16, and 4_trt_bf16 through the
    reused TensorRTEngine builder. This checkpoint runs the actual GPU builds — which
    cannot be unit-tested (they need TensorRT + the RTX 3070 + COCO val2017) — and
    confirms the architecture-agnostic builder produces valid engines for both YOLO
    graph topologies (YOLO11l DFL/C2PSA + NMS post-processing; YOLO26l NMS-free
    (1,300,6) output).
  </what-built>
  <how-to-verify>
    Run, from the repo root, the Stage 1-4 pipeline for both YOLO models (Stage 1 sets
    the FP32 baseline that Stage 3-4 accuracy_drop_pct is measured against; stages 2-4
    are the focus of this plan). Use a small `--limit` first to smoke-test, then the
    full run for real numbers:

    1. Smoke test (fast, confirms no crash):
       `uv run benchmark run --model yolo11l --stage 1_pytorch_fp32,2_onnx_fp32,3_trt_tf32,4_trt_fp16,4_trt_bf16 --limit 50`
       `uv run benchmark run --model yolo26l --stage 1_pytorch_fp32,2_onnx_fp32,3_trt_tf32,4_trt_fp16,4_trt_bf16 --limit 50`
    2. Full run (real metrics — reuses cached engines from the smoke test, so only
       re-measures): same two commands without `--limit`.
    3. Inspect `results/results.csv`. Confirm:
       - Rows exist for `model_name=yolo11l` and `model_name=yolo26l` at stages
         `3_trt_tf32`, `4_trt_fp16`, and `4_trt_bf16`.
       - `3_trt_tf32` and `4_trt_fp16` rows have a non-zero `map_50` and `map_50_95`
         (the engine actually detects objects — not all-zero output).
       - `4_trt_bf16` either has real metrics OR a populated `skipped_reason` (BF16 is
         allowed to gracefully skip; on the RTX 3070 / Ampere it is expected to build).
       - `engines/` now contains `yolo11l_tf32.engine`, `yolo11l_fp16.engine`,
         `yolo26l_tf32.engine`, `yolo26l_fp16.engine` (and `*_bf16.engine` if BF16 built)
         — and the existing `rtdetr_*.engine` files are still present and untouched.
    4. Confirm TRT build logs show `WORKSPACE` set to 2 GB (the builder logs this) and
       no "out of memory" errors (8 GB VRAM, 2 GB workspace cap — D-06).

    If a YOLO TRT build fails: capture the TRT parser/builder error and describe it —
    this would be a real finding (a YOLO graph op the builder cannot handle) requiring a
    plan revision, not a silent skip.
  </how-to-verify>
  <resume-signal>
    Type "approved" if Stage 3-4 engines built for both YOLO models and results.csv has
    valid (non-zero map_50) TF32 + FP16 rows for each, with BF16 either built or
    cleanly skipped. Otherwise describe the build failure or the invalid metric so the
    plan can be revised.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ONNX file → TensorRT `OnnxParser` → serialized engine | The YOLO ONNX graph (produced in Plan 01) is parsed and compiled by the TRT builder; a malformed graph could crash the builder or yield an invalid engine. |
| Serialized `.engine` file → `deserialize_cuda_engine` | Cached `.engine` files are deserialized and executed on subsequent runs without re-validation. |
| CLI args (`--engine-dir`, `--limit`) → filesystem + resource usage | User-supplied engine directory and image limit drive disk writes and GPU memory pressure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | YOLO ONNX graph parsed by `trt.OnnxParser` | mitigate | The ONNX file is validated with `onnx.checker.check_model` in Plan 01's `export_yolo_to_onnx` before it ever reaches the TRT parser; `_build_engine` additionally checks `parser.parse()` and raises `RuntimeError` with the parser errors if the graph is rejected, so a bad graph fails loudly rather than producing a corrupt engine. |
| T-07-06 | Tampering | Cached `.engine` file deserialized by `deserialize_cuda_engine` | accept | Engine files are produced locally by this same tool on this same machine and stored in an operator-controlled `engine_dir` (`.resolve()`-d in cli.py). There is no untrusted-engine ingestion path; treating a locally-built engine as trusted is proportionate for a single-user academic benchmark (ASVS L1). |
| T-07-07 | Denial of Service | TRT build exceeding the 8 GB VRAM budget | mitigate | `config.set_memory_pool_limit(trt.MemoryPoolType.WORKSPACE, 2 << 30)` strictly caps the build workspace at 2 GB (D-06), and `BaseEngine.reset_vram_tracking()` clears the CUDA cache between engine builds — preventing one stage's allocation from starving the next. The checkpoint explicitly verifies no OOM occurs. |
</threat_model>

<verification>
- `uv run pytest tests/test_tensorrt_engine.py` is green; full `uv run pytest tests/` does not regress.
- `uv run ruff check tests/` exits 0.
- `results/results.csv` contains `3_trt_tf32`, `4_trt_fp16`, `4_trt_bf16` rows for both `yolo11l` and `yolo26l` (verified at the checkpoint).
- TF32 and FP16 rows have non-zero `map_50` for both models; BF16 has real metrics or a `skipped_reason`.
- `engines/` contains the four (or six, if BF16 built) model-scoped YOLO engine files and the pre-existing `rtdetr_*.engine` files are intact.
</verification>

<success_criteria>
- TensorRT TF32, FP16, and BF16 engines build for YOLO11l and YOLO26l using the reused v1.0 builder, with the strict 2 GB workspace (D-04, D-05, D-06).
- BF16 is correctly gated on the Ampere `platform_has_tf32` check; a skip is recorded with `skipped_reason` if hardware support is absent (D-05).
- Stage 3-4 per-stage metrics are logged to `results.csv` / `results.json` with `model_name` and `stage` columns (OPT-YOLO-02, OPT-YOLO-05, D-16).
- The TRT engine unit test pins the YOLO build contract; full suite green, ruff clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-yolo-family-quantization-stages-2-6/07-02-SUMMARY.md`
</output>
