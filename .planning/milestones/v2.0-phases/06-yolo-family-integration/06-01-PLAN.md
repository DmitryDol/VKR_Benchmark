---
phase: 06-yolo-family-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/benchmark/engines/pytorch_engine.py, src/benchmark/models/rtdetr_adapter.py, tests/test_pytorch_engine.py]
autonomous: true
requirements: [ADPT-04, ADPT-08]
must_haves:
  truths:
    - "PyTorchEngine can run inference without hardcoded 'pixel_values' keyword"
  artifacts:
    - path: "src/benchmark/engines/pytorch_engine.py"
      provides: "Generic ModelAdapter protocol with infer() method"
    - path: "src/benchmark/models/rtdetr_adapter.py"
      provides: "RTDETRAdapter implementation of infer()"
    - path: "tests/test_pytorch_engine.py"
      provides: "Unit test for PyTorchEngine delegation"
---

<objective>
Refactor the PyTorch inference engine to support generic model adapters. 
This removes the architecture-specific 'pixel_values' keyword from the core engine logic, 
enabling support for YOLO and other models that expect positional arguments or different keywords.
This plan also establishes formal unit testing for the PyTorchEngine to ensure correct delegation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v2.0-phases/06-yolo-family-integration/06-CONTEXT.md
@src/benchmark/engines/pytorch_engine.py
@src/benchmark/models/rtdetr_adapter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit test for PyTorchEngine delegation</name>
  <files>tests/test_pytorch_engine.py</files>
  <behavior>
    - The test must verify that `PyTorchEngine.infer` delegates the forward pass to `ModelAdapter.infer`.
    - Use a mock model and a mock adapter to verify the call.
    - Test should fail initially because `PyTorchEngine` currently uses `self._model(pixel_values=inputs)`.
  </behavior>
  <action>
    Create `tests/test_pytorch_engine.py`:
    1. Define a unit test that mocks `torch.nn.Module` and a class conforming to `ModelAdapter`.
    2. Instantiate `PyTorchEngine` with these mocks.
    3. Call `engine.infer(mock_inputs)`.
    4. Assert that `mock_adapter.infer` was called with the mock model and mock inputs.
  </action>
  <verify>
    <automated>pytest tests/test_pytorch_engine.py</automated>
  </verify>
  <done>Failing test for PyTorchEngine delegation is created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor PyTorchEngine and genericize comments</name>
  <files>src/benchmark/engines/pytorch_engine.py</files>
  <behavior>
    - The `ModelAdapter` protocol must define an `infer` method with signature:
      `def infer(self, model: nn.Module, inputs: torch.Tensor) -> object:`
    - `PyTorchEngine.infer` must call `self._adapter.infer(self._model, inputs)`.
    - Comments in `pytorch_engine.py` referring specifically to RT-DETR or 'pixel_values' should be genericized to refer to "the model" or "inputs".
  </behavior>
  <action>
    Modify `src/benchmark/engines/pytorch_engine.py`:
    1. Update the `ModelAdapter` protocol to include the `infer` method.
    2. Update `PyTorchEngine.infer` to delegate the forward pass to the adapter.
    3. Review and genericize comments (per checker feedback) to remove RT-DETR specific terminology from the generic engine.
  </action>
  <verify>
    <automated>pytest tests/test_pytorch_engine.py</automated>
  </verify>
  <done>PyTorchEngine is refactored and comments are genericized. Test passes for delegation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement infer in RTDETRAdapter</name>
  <files>src/benchmark/models/rtdetr_adapter.py</files>
  <behavior>
    - `RTDETRAdapter` must implement the `infer` method.
    - It must perform the forward pass using `model(pixel_values=inputs)` to maintain compatibility with HuggingFace Transformers RT-DETR.
  </behavior>
  <action>
    Modify `src/benchmark/models/rtdetr_adapter.py`:
    1. Add the `infer` method to the `RTDETRAdapter` class.
    2. Ensure it uses the `pixel_values` keyword argument as required by the underlying HF model.
  </action>
  <verify>
    <automated>pytest tests/test_rtdetr_adapter.py tests/test_pytorch_engine.py</automated>
  </verify>
  <done>RTDETRAdapter is updated and existing benchmarks pass with the new delegation logic.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PyTorchEngine | mitigate | Ensure adapter `infer` is strictly typed and handles model states correctly. |
</threat_model>

<success_criteria>
PyTorchEngine successfully delegates inference to the adapter, verified by unit tests, and maintains backward compatibility with RT-DETR.
</success_criteria>

<output>
After completion, create `.planning/phases/06-yolo-family-integration/06-01-SUMMARY.md`
</output>
