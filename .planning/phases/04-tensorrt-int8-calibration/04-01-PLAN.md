---
phase: 04-tensorrt-int8-calibration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/benchmark/engines/int8_calibrators.py
  - src/benchmark/engines/tensorrt_engine.py
  - src/benchmark/engines/__init__.py
  - src/benchmark/cli.py
autonomous: true
requirements:
  - STAGE-5-MINMAX
  - STAGE-5-ENTROPY
  - STAGE-5-PERCENTILE
  - STAGE-5-BENCHMARK
  - STAGE-5-BEST

must_haves:
  truths:
    - "MinMaxCalibrator builds a valid INT8 TRT engine at engines/rtdetr_int8_minmax.engine"
    - "EntropyCalibrator builds a valid INT8 TRT engine at engines/rtdetr_int8_entropy.engine"
    - "PercentileCalibrator (quantile=0.9999) builds a valid INT8 TRT engine at engines/rtdetr_int8_percentile.engine"
    - "All three engines run full benchmark protocol and write stage rows to results/results.csv and results/results.json"
    - "Stage IDs in CSV/JSON are exactly: 5_trt_int8_minmax, 5_trt_int8_entropy, 5_trt_int8_percentile"
    - "Calibration tables persist to engines/rtdetr_int8_{method}.cache and are reused on subsequent builds"
    - "--force-rebuild deletes both .engine and .cache files before rebuilding"
  artifacts:
    - path: "src/benchmark/engines/int8_calibrators.py"
      provides: "MinMaxCalibrator, EntropyCalibrator, PercentileCalibrator, load_calibration_data"
      exports:
        - MinMaxCalibrator
        - EntropyCalibrator
        - PercentileCalibrator
        - load_calibration_data
    - path: "src/benchmark/engines/tensorrt_engine.py"
      provides: "Extended TensorRTEngine with precision='int8' and calibrator_method parameter"
      contains: "int8"
    - path: "src/benchmark/cli.py"
      provides: "STAGE_REGISTRY with 5_trt_int8_minmax, 5_trt_int8_entropy, 5_trt_int8_percentile"
      contains: "5_trt_int8_minmax"
  key_links:
    - from: "src/benchmark/engines/int8_calibrators.py"
      to: "src/benchmark/engines/tensorrt_engine.py"
      via: "_build_engine INT8 branch calls _make_calibrator()"
      pattern: "_make_calibrator"
    - from: "src/benchmark/engines/tensorrt_engine.py"
      to: "engines/rtdetr_int8_{method}.engine"
      via: "builder.build_serialized_network writes engine file"
      pattern: "int8.*\\.engine"
    - from: "src/benchmark/cli.py"
      to: "src/benchmark/engines/tensorrt_engine.py"
      via: "_run_stage instantiates TensorRTEngine(precision='int8', calibrator_method=...)"
      pattern: "calibrator_method"
---

<objective>
Implement TensorRT INT8 calibration (Stage 5) — three calibrator classes and full pipeline wiring.

Purpose: Stage 5 of the optimization pipeline requires three independent INT8 engines built with
MinMax, Entropy, and Percentile calibration. Each engine must be benchmarkable via the existing
protocol and produce per-stage CSV/JSON rows.

Output:
- src/benchmark/engines/int8_calibrators.py (new)
- src/benchmark/engines/tensorrt_engine.py (extended)
- src/benchmark/engines/__init__.py (updated exports)
- src/benchmark/cli.py (three new stage branches)
</objective>

<execution_context>
@C:/Users/dmitr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dmitr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projects/ResearchSSAU/VKR_Claude/.planning/PROJECT.md
@C:/projects/ResearchSSAU/VKR_Claude/.planning/ROADMAP.md
@C:/projects/ResearchSSAU/VKR_Claude/.planning/STATE.md
@C:/projects/ResearchSSAU/VKR_Claude/src/benchmark/engines/tensorrt_engine.py
@C:/projects/ResearchSSAU/VKR_Claude/src/benchmark/cli.py
@C:/projects/ResearchSSAU/VKR_Claude/.planning/phases/04-tensorrt-int8-calibration/04-CONTEXT.md
@C:/projects/ResearchSSAU/VKR_Claude/.planning/phases/04-tensorrt-int8-calibration/04-RESEARCH.md

<interfaces>
<!-- Existing TensorRTEngine contract the executor extends. Do not re-read the file. -->

From src/benchmark/engines/tensorrt_engine.py:

```python
class TensorRTEngine(BaseEngine):
    def __init__(
        self,
        model_name: str,
        precision: Literal["tf32", "fp16", "bf16"],   # ← extend to include "int8"
        engine_dir: Path,
        force_rebuild: bool = False,
        score_threshold: float = 0.01,
    ) -> None: ...

    def load_model(self, weights_path: Path) -> None: ...
    # For INT8: signature extends to accept optional calibration dataloader

    def _build_engine(self, onnx_path: Path) -> None: ...
    # Has tf32/fp16/bf16 branches; needs INT8 branch per D-01/Critical Technical Facts

    # Private fields already present:
    self._engine_dir: Path
    self._force_rebuild: bool
    self._engine_path: Path   # engine_dir / f"rtdetr_{precision}.engine"
    self._skipped_reason: str
```

From src/benchmark/cli.py:

```python
STAGE_REGISTRY: list[str] = [
    "1_pytorch_fp32", "2_onnx_fp32",
    "3_trt_tf32", "4_trt_fp16", "4_trt_bf16",
    # Phase 4 appends: "5_trt_int8_minmax", "5_trt_int8_entropy", "5_trt_int8_percentile"
]

def _run_stage(
    model_name: str, stage: str, limit: int | None,
    result_logger: ResultLogger, baseline_map: float,
    macs: float | None, flops: float | None,
    engine_dir: Path = Path("engines"),
    force_rebuild: bool = False,
) -> tuple[float, float, float]: ...
# Has elif stage in ("3_trt_tf32", "4_trt_fp16", "4_trt_bf16"): block to follow as pattern
```

From src/benchmark/engines/__init__.py (current exports):
```python
# TensorRTEngine is already exported; calibrator classes need adding
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create int8_calibrators.py with three calibrator classes and shared data loader</name>
  <files>src/benchmark/engines/int8_calibrators.py</files>
  <behavior>
    - load_calibration_data(dataloader, n=500): returns list of 500 (1,3,640,640) float32 CPU tensors,
      first 500 images in dataloader iteration order, shuffle=False, same preprocessing as
      TensorRTEngine.preprocess (resize to 640x640, /255.0, transpose to CHW, no ImageNet norm).
    - MinMaxCalibrator.__init__(data, cache_path): calls super().__init__(), stores data list and
      cache_path, initializes self._batch_idx=0, self._device_buf=None.
    - MinMaxCalibrator.get_batch_size(): returns 8 (D-08).
    - MinMaxCalibrator.get_batch(names): batches data[idx*8:(idx+1)*8] into (8,3,640,640) GPU tensor,
      stores as self._device_buf (GC protection), increments _batch_idx, returns [self._device_buf.data_ptr()].
      Returns None when data exhausted.
    - MinMaxCalibrator.read_calibration_cache(): returns cache_path.read_bytes() if exists else None.
    - MinMaxCalibrator.write_calibration_cache(cache): cache_path.write_bytes(cache).
    - EntropyCalibrator: identical structure but inherits trt.IInt8EntropyCalibrator2.
    - PercentileCalibrator: inherits trt.IInt8LegacyCalibrator, sets self.quantile=0.9999 and
      self.regression_cutoff=1.0 in __init__ (property assignments, not method overrides).
      get_algorithm() is NOT overridden (correct default per base class).
    - All three reset _batch_idx to 0 at start (for potential re-use).
    - Module has module-level logger = logging.getLogger(__name__).
    - Private helper _make_calibrator(method, dataloader, cache_path) returns correct instance
      by method string: "minmax"->MinMaxCalibrator, "entropy"->EntropyCalibrator,
      "percentile"->PercentileCalibrator.
  </behavior>
  <action>
    Create `src/benchmark/engines/int8_calibrators.py` as a new module.

    Header:
    ```python
    """INT8 calibrator classes for TensorRT engine builds (Stage 5).

    Three calibration strategies: MinMax, Entropy (2), and Percentile (Legacy).
    All use 500 COCO val2017 images in deterministic order with calibration batch size 8.
    """

    from __future__ import annotations

    import logging
    from typing import TYPE_CHECKING, Literal

    import torch

    try:
        import tensorrt as trt  # type: ignore[import-untyped]
    except ImportError:
        trt = None  # type: ignore[assignment]

    if TYPE_CHECKING:
        from pathlib import Path
        from benchmark.data.coco_loader import COCODataLoader

    logger = logging.getLogger(__name__)

    _CAL_BATCH_SIZE: int = 8
    _CAL_IMAGES: int = 500
    _INPUT_SIZE: tuple[int, int] = (640, 640)
    ```

    Implement `load_calibration_data(dataloader: COCODataLoader, n: int = _CAL_IMAGES) -> list[torch.Tensor]`:
    - Iterates dataloader (no shuffle, deterministic order per D-06/D-07).
    - For each COCOSample: PIL resize to 640x640 BILINEAR, /255.0 float32, transpose CHW,
      unsqueeze(0) → shape (1,3,640,640) on CPU.
    - Collects first n tensors. Logs info at start and completion.
    - Raises RuntimeError if fewer than n images available (guard against misconfigured dataloader).

    Implement `_MinMaxMixin` as a mixin (or implement all three classes directly — choose direct
    classes for clarity, as the base classes differ):

    `class MinMaxCalibrator(trt.IInt8MinMaxCalibrator)`:
    ```
    def __init__(self, data: list[torch.Tensor], cache_path: Path) -> None:
        super().__init__()
        self._data = data
        self._cache_path = cache_path
        self._batch_idx: int = 0
        self._device_buf: torch.Tensor | None = None

    def get_batch_size(self) -> int: return _CAL_BATCH_SIZE

    def get_batch(self, names: list[str]) -> list[int] | None:
        start = self._batch_idx * _CAL_BATCH_SIZE
        if start >= len(self._data):
            return None
        batch = self._data[start : start + _CAL_BATCH_SIZE]
        # Pad last batch if shorter than _CAL_BATCH_SIZE
        if len(batch) < _CAL_BATCH_SIZE:
            batch = batch + [batch[-1]] * (_CAL_BATCH_SIZE - len(batch))
        self._device_buf = torch.cat(batch, dim=0).cuda()  # (8,3,640,640)
        self._batch_idx += 1
        return [self._device_buf.data_ptr()]

    def read_calibration_cache(self) -> bytes | None:
        if self._cache_path.exists():
            logger.info("Reading calibration cache: %s", self._cache_path)
            return self._cache_path.read_bytes()
        return None

    def write_calibration_cache(self, cache: bytes) -> None:
        self._cache_path.parent.mkdir(parents=True, exist_ok=True)
        self._cache_path.write_bytes(cache)
        logger.info("Calibration cache written: %s", self._cache_path)
    ```

    `class EntropyCalibrator(trt.IInt8EntropyCalibrator2)`: identical body to MinMaxCalibrator.

    `class PercentileCalibrator(trt.IInt8LegacyCalibrator)`:
    - Same body as MinMaxCalibrator PLUS in `__init__` after super().__init__():
      ```python
      self.quantile: float = 0.9999
      self.regression_cutoff: float = 1.0
      ```
    - Do NOT override get_algorithm().

    Implement `_make_calibrator`:
    ```python
    def _make_calibrator(
        method: Literal["minmax", "entropy", "percentile"],
        dataloader: COCODataLoader,
        cache_path: Path,
    ) -> MinMaxCalibrator | EntropyCalibrator | PercentileCalibrator:
        data = load_calibration_data(dataloader)
        if method == "minmax":
            return MinMaxCalibrator(data, cache_path)
        if method == "entropy":
            return EntropyCalibrator(data, cache_path)
        if method == "percentile":
            return PercentileCalibrator(data, cache_path)
        msg = f"Unknown calibrator method: {method!r}"
        raise ValueError(msg)
    ```

    Ruff compliance:
    - `from __future__ import annotations` at top.
    - All parameters and returns fully annotated.
    - No print statements; use logger.
    - Use `msg` variable + raise pattern throughout.
    - `TYPE_CHECKING` guard for COCODataLoader and Path imports.

    Run `ruff check src/benchmark/engines/int8_calibrators.py` and `ruff format src/benchmark/engines/int8_calibrators.py` after writing. Fix all reported issues before marking done.
  </action>
  <verify>
    <automated>cd /c/projects/ResearchSSAU/VKR_Claude && uv run ruff check src/benchmark/engines/int8_calibrators.py && uv run ruff format --check src/benchmark/engines/int8_calibrators.py && uv run python -c "from benchmark.engines.int8_calibrators import MinMaxCalibrator, EntropyCalibrator, PercentileCalibrator, load_calibration_data, _make_calibrator; print('imports OK')"</automated>
  </verify>
  <done>
    - int8_calibrators.py exists with all three calibrator classes and load_calibration_data helper.
    - ruff check passes with zero errors, ruff format --check passes.
    - Import smoke test prints "imports OK" without raising.
    - PercentileCalibrator has quantile=0.9999 and regression_cutoff=1.0 set in __init__.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend TensorRTEngine for INT8 precision and update CLI stage wiring</name>
  <files>
    src/benchmark/engines/tensorrt_engine.py,
    src/benchmark/engines/__init__.py,
    src/benchmark/cli.py
  </files>
  <action>
    ### A. src/benchmark/engines/tensorrt_engine.py

    **1. Update `__init__` signature (per D-01):**

    Change `precision: Literal["tf32", "fp16", "bf16"]` to
    `precision: Literal["tf32", "fp16", "bf16", "int8"]`.

    Add new parameter after `force_rebuild`:
    ```python
    calibrator_method: Literal["minmax", "entropy", "percentile"] | None = None,
    ```

    Store as `self._calibrator_method = calibrator_method`.

    Update engine path logic: when precision == "int8", the path must encode the method:
    ```python
    if precision == "int8":
        if calibrator_method is None:
            msg = "calibrator_method is required when precision='int8'"
            raise ValueError(msg)
        self._engine_path = engine_dir / f"rtdetr_int8_{calibrator_method}.engine"
    else:
        self._engine_path = engine_dir / f"rtdetr_{precision}.engine"
    ```

    Also store the cache path for INT8:
    ```python
    if precision == "int8":
        self._cache_path: Path | None = engine_dir / f"rtdetr_int8_{calibrator_method}.cache"
    else:
        self._cache_path = None
    ```

    **2. Update `load_model` signature (per D-01):**

    Extend to accept optional calibration dataloader:
    ```python
    def load_model(
        self,
        weights_path: Path,
        calibration_dataloader: COCODataLoader | None = None,
    ) -> None:
    ```

    Store for use in _build_engine: `self._calibration_dataloader = calibration_dataloader`.

    For INT8 precision, guard that calibration_dataloader is provided when a build is needed:
    ```python
    if self.precision == "int8" and not self._engine_path.exists():
        if calibration_dataloader is None:
            msg = "calibration_dataloader required to build INT8 engine"
            raise ValueError(msg)
    ```

    Place guard before the `if not self._force_rebuild and self._engine_path.exists()` check.

    **Force rebuild (per D-05):** When `self._force_rebuild` is True and precision == "int8",
    delete BOTH engine file and cache file before proceeding:
    ```python
    if self._force_rebuild:
        if self._engine_path.exists():
            self._engine_path.unlink()
            logger.info("Deleted cached engine: %s", self._engine_path)
        if self._cache_path is not None and self._cache_path.exists():
            self._cache_path.unlink()
            logger.info("Deleted calibration cache: %s", self._cache_path)
    ```
    Insert this block at the top of `load_model`, before the cache-hit check.
    (Existing non-INT8 precision: cache_path is None so the unlink is a no-op — safe.)

    **3. Add INT8 branch to `_build_engine`:**

    After the `elif self.precision == "bf16":` block, add:
    ```python
    elif self.precision == "int8":
        from benchmark.engines.int8_calibrators import _make_calibrator  # noqa: PLC0415

        config.set_flag(trt.BuilderFlag.INT8)

        if self._calibration_dataloader is None or self._cache_path is None:
            msg = "INT8 build requires calibration_dataloader and cache_path"
            raise RuntimeError(msg)

        calibrator = _make_calibrator(
            self._calibrator_method,  # type: ignore[arg-type]
            self._calibration_dataloader,
            self._cache_path,
        )
        config.int8_calibrator = calibrator

        # Calibration profile: same input name, batch=8 (per Critical Technical Facts)
        cal_profile = builder.create_optimization_profile()
        for i in range(network.num_inputs):
            inp = network.get_input(i)
            inp_shape = inp.shape  # e.g. (-1, 3, 640, 640)
            cal_fixed = tuple(8 if d == -1 else d for d in inp_shape)
            cal_profile.set_shape(inp.name, min=cal_fixed, opt=cal_fixed, max=cal_fixed)
        config.set_calibration_profile(cal_profile)
    ```

    The existing inference optimization profile (batch=1) block runs unconditionally above
    the precision branches — INT8 gets BOTH profiles (inference profile already added, then
    calibration profile added here). This satisfies the "two optimization profiles" gotcha.

    **4. Update docstring** of `TensorRTEngine` to mention `int8` precision and `calibrator_method`.

    ### B. src/benchmark/engines/__init__.py

    Read the file first. Add exports for the three calibrator classes:
    ```python
    from benchmark.engines.int8_calibrators import (
        EntropyCalibrator,
        MinMaxCalibrator,
        PercentileCalibrator,
    )
    ```
    Add to `__all__` if it exists.

    ### C. src/benchmark/cli.py

    **1. Extend STAGE_REGISTRY** — append the three INT8 stages (per D-02/D-08):
    ```python
    STAGE_REGISTRY: list[str] = [
        "1_pytorch_fp32",
        "2_onnx_fp32",
        "3_trt_tf32",
        "4_trt_fp16",
        "4_trt_bf16",
        "5_trt_int8_minmax",
        "5_trt_int8_entropy",
        "5_trt_int8_percentile",
    ]
    ```

    **2. Add INT8 elif branch in `_run_stage`** after the existing TRT block:
    ```python
    elif stage in ("5_trt_int8_minmax", "5_trt_int8_entropy", "5_trt_int8_percentile"):
        cal_method_map = {
            "5_trt_int8_minmax": "minmax",
            "5_trt_int8_entropy": "entropy",
            "5_trt_int8_percentile": "percentile",
        }
        cal_method = cal_method_map[stage]
        onnx_path = Path(MODEL_REGISTRY[model_name]["onnx"])
        if not onnx_path.exists():
            msg = f"ONNX model missing: {onnx_path} — run stage 2 first"
            raise FileNotFoundError(msg)

        # 500-image calibration dataloader, no limit override (D-06: first 500 in order)
        cal_dataloader = COCODataLoader(limit=500)

        engine = TensorRTEngine(
            model_name=model_name,
            precision="int8",
            calibrator_method=cal_method,  # type: ignore[arg-type]
            engine_dir=engine_dir,
            force_rebuild=force_rebuild,
        )
        engine.load_model(onnx_path, calibration_dataloader=cal_dataloader)

        result = engine.run_full_benchmark(
            dataloader,
            stage=stage,
            baseline_map_50_95=baseline_map,
            macs=macs,
            flops=flops,
        )
    ```

    Note: `dataloader` is the full benchmark dataloader (uses whatever `limit` the user passed),
    `cal_dataloader` is the fixed 500-image calibration set.

    Ruff compliance on all three files: run `ruff check` and `ruff format --check` after edits.
    Use local import `from benchmark.engines.int8_calibrators import _make_calibrator` inside
    `_build_engine` to avoid circular import at module level (TRT optional import pattern).
    Annotate `self._calibrator_method` with `Literal["minmax","entropy","percentile"] | None`.
    Add `COCODataLoader` to the TYPE_CHECKING import block in tensorrt_engine.py — it is already
    there; ensure `calibration_dataloader: COCODataLoader | None` is covered.
  </action>
  <verify>
    <automated>cd /c/projects/ResearchSSAU/VKR_Claude && uv run ruff check src/benchmark/engines/tensorrt_engine.py src/benchmark/engines/__init__.py src/benchmark/cli.py && uv run ruff format --check src/benchmark/engines/tensorrt_engine.py src/benchmark/engines/__init__.py src/benchmark/cli.py && uv run python -c "from benchmark.engines.tensorrt_engine import TensorRTEngine; from benchmark.cli import STAGE_REGISTRY; assert '5_trt_int8_minmax' in STAGE_REGISTRY; assert '5_trt_int8_entropy' in STAGE_REGISTRY; assert '5_trt_int8_percentile' in STAGE_REGISTRY; print('wiring OK')"</automated>
  </verify>
  <done>
    - TensorRTEngine.__init__ accepts precision='int8' and calibrator_method parameter.
    - Engine path for int8 encodes calibrator method: rtdetr_int8_{method}.engine.
    - Cache path stored as rtdetr_int8_{method}.cache; --force-rebuild deletes both.
    - _build_engine has INT8 branch: sets INT8 flag, attaches calibrator, adds calibration
      profile (batch=8) in addition to inference profile (batch=1).
    - load_model signature accepts calibration_dataloader kwarg.
    - STAGE_REGISTRY in cli.py has all three 5_trt_int8_* stage IDs.
    - _run_stage handles all three INT8 stages with correct calibrator_method routing.
    - ruff check and ruff format --check pass on all three files.
    - Import smoke test prints "wiring OK".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 created int8_calibrators.py with MinMaxCalibrator, EntropyCalibrator, and
    PercentileCalibrator. Task 2 extended TensorRTEngine and wired CLI stage branches.
    All ruff checks pass; import smoke tests pass.

    To run a full INT8 engine build for minmax (reads existing ONNX, builds engine,
    writes cache, benchmarks 5 images as a smoke test):

    ```
    cd C:\projects\ResearchSSAU\VKR_Claude
    uv run python -m benchmark.cli run --model rt-detr --stage 5_trt_int8_minmax --limit 5
    ```

    Expected: TRT builds engines/rtdetr_int8_minmax.engine, writes engines/rtdetr_int8_minmax.cache,
    runs 50 warm-up + 5 measured iterations (limited by --limit 5), writes per-stage CSV/JSON.

    For entropy and percentile (run only after minmax succeeds):
    ```
    uv run python -m benchmark.cli run --model rt-detr --stage 5_trt_int8_entropy --limit 5
    uv run python -m benchmark.cli run --model rt-detr --stage 5_trt_int8_percentile --limit 5
    ```

    Check results/ directory for: results_5_trt_int8_minmax.csv, results_5_trt_int8_entropy.csv,
    results_5_trt_int8_percentile.csv with non-NaN latency and mAP values.
  </what-built>
  <how-to-verify>
    1. Run minmax stage command above. Confirm engine file exists:
       `ls engines/rtdetr_int8_minmax.engine engines/rtdetr_int8_minmax.cache`
    2. Run entropy and percentile stages. Confirm all three .engine and .cache files exist.
    3. Check per-stage CSVs: each must have a row where `stage` column matches the stage ID
       and `latency_inference_ms` is a positive number (not NaN, not 0).
    4. Run full pipeline merge:
       `uv run python -m benchmark.cli merge --model rt-detr`
       Confirm results/results.csv contains rows for all five stages (3_trt_tf32 through
       5_trt_int8_percentile) if prior phase engines exist, or at minimum the three INT8 rows.
    5. Verify cache reuse: run minmax stage again WITHOUT --force-rebuild. Build log should say
       "Loading cached TRT engine" and NOT "Building TRT int8 engine".
    6. Verify force-rebuild: run with --force-rebuild. Log should say "Deleted cached engine"
       and "Deleted calibration cache" before rebuilding.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all three engines build, benchmark, and produce non-NaN CSV rows.
    Or describe the failure (error message + which stage failed) so I can diagnose.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calibration data → GPU tensor | 500 COCO images preprocessed and moved to GPU device memory during get_batch() |
| cache file → disk | Calibration tables written as raw bytes to engines/ directory |
| ONNX path → TRT builder | Builder reads ONNX bytes from disk, parses graph |
| CLI --engine-dir / --output-dir → filesystem | User-supplied paths resolved before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | engines/*.cache | accept | Cache files contain quantized activation statistics — no PII, no secrets. Low-value target. Files are co-located with engine binaries already present. |
| T-04-02 | Denial of Service | VRAM during calibration | mitigate | Calibration batch=8 with 500 images = 63 passes × (8,3,640,640) float32 ≈ 375 MB peak additional VRAM. On RTX 3070 (8 GB) this is within budget. Monitor: if OOM occurs, reduce _CAL_BATCH_SIZE to 4. |
| T-04-03 | Tampering | engines/*.cache corruption | mitigate | TRT validates cache on read; invalid bytes cause build failure with TRT error (not silent wrong results). --force-rebuild provides clean-slate recovery path (D-05). |
| T-04-04 | Spoofing | GC collecting device_buf before TRT reads pointer | mitigate | self._device_buf stored as instance attribute in all three calibrators (Critical Technical Fact #1). This is the primary correctness risk — task action explicitly addresses it. |
| T-04-05 | Information Disclosure | Path traversal via --engine-dir | accept | cli.py already calls engine_dir.resolve() (inherited from Phase 3, T-03-06). Mitigation already in place. |
| T-04-06 | Denial of Service | 500-image calibration build time | accept | Expected: 5-15 min on RTX 3070. Documented as expected behavior. Cache (D-04) eliminates cost on subsequent runs. |
</threat_model>

<verification>
Overall phase checks (run after all tasks complete):

```bash
# 1. Ruff clean on all modified files
uv run ruff check src/benchmark/engines/int8_calibrators.py src/benchmark/engines/tensorrt_engine.py src/benchmark/cli.py
uv run ruff format --check src/benchmark/engines/int8_calibrators.py src/benchmark/engines/tensorrt_engine.py src/benchmark/cli.py

# 2. All three stage IDs in registry
uv run python -c "from benchmark.cli import STAGE_REGISTRY; assert all(s in STAGE_REGISTRY for s in ['5_trt_int8_minmax','5_trt_int8_entropy','5_trt_int8_percentile']); print('registry OK')"

# 3. Calibrator imports
uv run python -c "from benchmark.engines.int8_calibrators import MinMaxCalibrator, EntropyCalibrator, PercentileCalibrator; print('calibrators OK')"

# 4. Engine files and caches exist after builds
ls engines/rtdetr_int8_minmax.engine engines/rtdetr_int8_entropy.engine engines/rtdetr_int8_percentile.engine
ls engines/rtdetr_int8_minmax.cache engines/rtdetr_int8_entropy.cache engines/rtdetr_int8_percentile.cache

# 5. Per-stage result files exist with correct stage IDs
python -c "
import csv, pathlib
for method in ['minmax','entropy','percentile']:
    f = pathlib.Path(f'results/results_5_trt_int8_{method}.csv')
    assert f.exists(), f'{f} missing'
    rows = list(csv.DictReader(f.open()))
    assert rows, f'{f} empty'
    assert rows[0]['stage'] == f'5_trt_int8_{method}', f'wrong stage in {f}'
    lat = float(rows[0]['latency_inference_ms'])
    assert lat > 0 and lat == lat, f'NaN or zero latency in {f}'
    print(f'{method}: latency={lat:.2f}ms OK')
print('all CSV checks passed')
"
```
</verification>

<success_criteria>
Phase is complete when:
1. engines/rtdetr_int8_minmax.engine exists and is > 0 bytes.
2. engines/rtdetr_int8_entropy.engine exists and is > 0 bytes.
3. engines/rtdetr_int8_percentile.engine exists and is > 0 bytes.
4. engines/rtdetr_int8_{method}.cache exists for all three methods.
5. Per-stage CSV files exist with rows containing positive (non-NaN) latency_inference_ms.
6. Stage IDs in CSV rows exactly match: 5_trt_int8_minmax, 5_trt_int8_entropy, 5_trt_int8_percentile.
7. Cache reuse works: second run without --force-rebuild logs "Loading cached TRT engine".
8. --force-rebuild deletes both .engine and .cache before rebuilding (verified in logs).
9. ruff check passes with zero errors on all four modified files.
10. Best calibrator by mAP_50:95 is identifiable by comparing the three per-stage CSV rows.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tensorrt-int8-calibration/04-01-SUMMARY.md`
following the template at `@C:/Users/dmitr/.claude/get-shit-done/templates/summary.md`.

Include in the summary:
- Which calibrator produced the best mAP_50:95 (and the value)
- Calibration build time observed for each method
- Any VRAM pressure events or OOM errors encountered and how resolved
- Cache file sizes (bytes) for all three methods
</output>
