---
phase: 13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion
plan: 13.01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/benchmark/utils/logger.py
  - src/benchmark/engines/base.py
  - src/benchmark/eval/__init__.py
  - src/benchmark/eval/per_class.py
  - scripts/build_per_class_ap.py
  - .gitignore
  - results/rt-detr/quant/1_pytorch_fp32.json
  - results/rt-detr/quant/2_onnx_fp32.json
  - results/rt-detr/quant/3_trt_tf32.json
  - results/rt-detr/quant/4_trt_fp16.json
  - results/rt-detr/quant/4_trt_bf16.json
  - results/rt-detr/quant/5_trt_int8_entropy.json
  - results/rt-detr/quant/5_trt_int8_minmax.json
  - results/rt-detr/quant/5_trt_int8_percentile.json
  - results/rt-detr/quant/6_trt_mixed_a.json
  - results/rt-detr/quant/6_trt_mixed_b.json
  - results/yolo11l/quant/*.json (10 files)
  - results/yolo26l/quant/*.json (10 files)
  - results/rfdetr-l/rfdetr_v1/*.json (5 valid stages)
autonomous: true
requirements: [VKR-13-T1]

must_haves:
  truths:
    - "Every one of the 35 valid stage JSONs contains a per_class_ap field of length 80"
    - "Each per_class_ap entry has class_id (int), class_name (string, English), ap_50_95 (float), ap_50 (float), n_gt (int)"
    - "Per-class AP is sorted ascending by COCO-91 class_id"
    - "Re-running scripts/build_per_class_ap.py produces byte-identical JSON content for per_class_ap (sha256-stable)"
    - "cache/predictions/coco_dt_<model>_<stage>.json exists for each of the 35 valid configurations after the script runs"
    - "cache/ is gitignored"
  artifacts:
    - path: "src/benchmark/eval/__init__.py"
      provides: "Public exports for benchmark.eval package"
    - path: "src/benchmark/eval/per_class.py"
      provides: "Per-class AP postprocessor that reads cached coco_dt JSON and (re)computes per_class_ap without re-running inference"
      exports: ["compute_per_class_ap_from_cache", "compute_per_class_ap_from_results", "PerClassAPEntry"]
    - path: "scripts/build_per_class_ap.py"
      provides: "Typer CLI that runs/re-runs evaluate_accuracy on the 35 valid configs, populates cache, and rewrites stage JSONs in place"
    - path: "cache/predictions/coco_dt_<model>_<stage>.json"
      provides: "Cached COCO-format prediction list (35 files)"
  key_links:
    - from: "src/benchmark/engines/base.py::evaluate_accuracy"
      to: "src/benchmark/eval/per_class.py::compute_per_class_ap_from_results"
      via: "function call after coco_eval.accumulate()"
      pattern: "compute_per_class_ap_from_results"
    - from: "scripts/build_per_class_ap.py"
      to: "results/<model>/<variant>/<stage>.json"
      via: "json.loads → mutate per_class_ap → json.dumps with sort_keys=False and identical formatting"
      pattern: "per_class_ap"
---

<objective>
Add the per-class COCO AP (80 classes) infrastructure to the benchmark pipeline and backfill the 35 valid existing stage JSON reports with the new `per_class_ap` field. The TRT engines on disk are NOT rebuilt — we re-run `evaluate_accuracy()` against existing engines (and cached predictions where possible), extract per-class AP from `pycocotools.cocoeval.COCOeval.eval["precision"]`, cache the raw COCO-format prediction list to `cache/predictions/`, and rewrite the 35 existing stage JSONs in place adding only the new field.

Purpose: P02 (confusion matrices), P03 (per-class summary tables), and P04 (qualitative examples) all consume either the cached prediction list or the new `per_class_ap` field. Without P01, downstream plans cannot run.

Output:
- New `src/benchmark/eval/` package with `__init__.py` and `per_class.py`.
- Extended `BenchmarkResult` with `per_class_ap: list[dict[str, int | float | str]] = field(default_factory=list)`.
- Extended `BaseEngine.evaluate_accuracy()` that caches `coco_results` to `cache/predictions/coco_dt_<model>_<stage>.json` and extracts per-class AP from `coco_eval.eval["precision"]`.
- `scripts/build_per_class_ap.py` typer CLI.
- 35 updated stage JSON files with `per_class_ap` field of length 80.
- 35 cached prediction JSON files in `cache/predictions/`.
- `.gitignore` updated with `cache/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13-CONTEXT.md

@prompt4edits/code-agent-vkr-artifacts.md
@CLAUDE.md
@pyproject.toml

@src/benchmark/engines/base.py
@src/benchmark/utils/logger.py
@src/benchmark/data/coco_loader.py
@src/benchmark/cli.py

<interfaces>
<!-- Existing types the executor must use, extracted from the codebase. -->

From src/benchmark/data/coco_loader.py:
- `COCO_91_TO_80: dict[int, int]` — 80 entries, maps COCO-91 native ID → contiguous 80-class index
- `COCO_80_TO_91: dict[int, int]` — reverse mapping
- `class COCODataLoader` — has `.coco` property returning a `pycocotools.coco.COCO` object whose `.cats` dict has `{cat_id: {"id", "name", "supercategory"}}` for the 80 COCO categories
- `class COCOSample`, `class COCOAnnotation`

From src/benchmark/engines/base.py (current):
- `class BaseEngine(ABC)` — has `evaluate_accuracy(self, dataloader: COCODataLoader) -> dict[str, float]` returning the 12 mAP/AR stats.
- Inside `evaluate_accuracy`: `coco_results` (list[dict]) is built, then `coco_dt = dataloader.coco.loadRes(coco_results); coco_eval = COCOeval(dataloader.coco, coco_dt, "bbox"); coco_eval.evaluate(); coco_eval.accumulate(); coco_eval.summarize()`. After `accumulate()`, `coco_eval.eval["precision"]` is a numpy ndarray of shape `[T=10, R=101, K=80, A=4, M=3]` (T = IoU thresholds 0.50..0.95 step 0.05; R = recall points; K = category index in `coco_eval.params.catIds`; A = area range index; M = maxDets index).
- `self.model_name: str` and `self.engine_type: str` and `self.precision: str` are populated in `__init__`.

From src/benchmark/utils/logger.py (current):
- `@dataclass class BenchmarkResult` — 26 fields, plus `model_size_mb`, `vram_peak_mb`, etc. Add `per_class_ap: list[dict[str, int | float | str]] = field(default_factory=list)` AFTER `flops` and BEFORE the `hw_gpu` block (place it next to other accuracy-derived fields).
- `class ResultLogger` — `save_stage_files()` writes `{stage}.csv` and `{stage}.json` using `asdict(result)`. The CSV writer uses `csv.DictWriter(f, fieldnames=row.keys())` — that will turn `per_class_ap` (a list) into the repr `"[{...}]"` in the CSV. CSV does not need the field. **You must drop `per_class_ap` from the CSV row only** (pop the key from the dict before writing CSV; keep it in JSON).
- `_append_csv()` writes to the unified `results.csv` — same treatment, pop `per_class_ap` before writing.

Stage IDs (verbatim from src/benchmark/cli.py::STAGE_REGISTRY):
  `1_pytorch_fp32`, `2_onnx_fp32`, `3_trt_tf32`, `4_trt_fp16`, `4_trt_bf16`, `5_trt_int8_minmax`, `5_trt_int8_entropy`, `5_trt_int8_percentile`, `6_trt_mixed_a`, `6_trt_mixed_b`.

Per-model results variants on disk:
  `rt-detr` → `results/rt-detr/quant/` — 10 stage JSONs
  `yolo11l` → `results/yolo11l/quant/` — 10 stage JSONs
  `yolo26l` → `results/yolo26l/quant/` — 10 stage JSONs
  `rfdetr-l` → `results/rfdetr-l/rfdetr_v1/` — 10 stage JSONs (5 INVALID: `5_trt_int8_entropy`, `5_trt_int8_minmax`, `5_trt_int8_percentile`, `6_trt_mixed_a`, `6_trt_mixed_b` — these tactic-rolled back to FP16 and are EXCLUDED from per-class AP)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend BenchmarkResult and BaseEngine.evaluate_accuracy with per-class AP infrastructure</name>
  <files>src/benchmark/utils/logger.py, src/benchmark/engines/base.py, src/benchmark/eval/__init__.py, src/benchmark/eval/per_class.py, .gitignore</files>
  <read_first>
    - `.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13-CONTEXT.md` (authoritative scope — per-class AP semantics in `### Per-class AP semantics (locked)`)
    - `prompt4edits/code-agent-vkr-artifacts.md` (advisor spec — Task 1)
    - `src/benchmark/engines/base.py` (existing `evaluate_accuracy()` body — lines 152-218; you will add per-class extraction and prediction caching to this exact method, NO timing changes)
    - `src/benchmark/utils/logger.py` (existing `BenchmarkResult` dataclass and `ResultLogger._append_csv` / `save_stage_files` — you will add one field and gate `per_class_ap` out of CSV)
    - `src/benchmark/data/coco_loader.py` (for `COCO_91_TO_80`, `COCO_80_TO_91`, and how the COCO object is accessed via `dataloader.coco`)
    - `CLAUDE.md` (ruff strict mode rules; no comments unless WHY is non-obvious; `from __future__ import annotations` mandatory)
  </read_first>
  <action>
Implement the per-class AP infrastructure in three coordinated edits + one new package.

1) **Extend `src/benchmark/utils/logger.py::BenchmarkResult`**: Add a new dataclass field after `flops: float | None = None` and before the `hw_gpu: str = ""` block:
   ```
   per_class_ap: list[dict[str, int | float | str]] = field(default_factory=list)
   ```
   You MUST `from dataclasses import field` (already imported alongside `dataclass` — verify and add if missing).

   Then update CSV writers so `per_class_ap` is never written to CSV (it would become a Python repr string):
   - In `ResultLogger._append_csv(self, result)`: `row = asdict(result); row.pop("per_class_ap", None)` before constructing `DictWriter`.
   - In `ResultLogger.save_stage_files(self, result)`: build `row_csv = asdict(result); row_csv.pop("per_class_ap", None)` for the CSV write; keep `row = asdict(result)` (full) for the JSON write.
   - `merge_to_unified()` reads from existing per-stage CSVs (which never had per_class_ap) — no change needed there; do not strip in JSON merge.

2) **Create `src/benchmark/eval/__init__.py`**: Empty module-level docstring (`"""Postprocessing analytics for diploma artifacts (Phase 13)."""`) and an explicit `__all__ = ["compute_per_class_ap_from_results", "compute_per_class_ap_from_cache", "PerClassAPEntry"]` re-exported from `per_class.py`.

3) **Create `src/benchmark/eval/per_class.py`** with:
   - `from __future__ import annotations`, module-level `logger = logging.getLogger(__name__)`.
   - A `TypedDict` named `PerClassAPEntry` with keys `class_id: int`, `class_name: str`, `ap_50_95: float`, `ap_50: float`, `n_gt: int`.
   - Function `compute_per_class_ap_from_results(coco_eval: COCOeval, coco: COCO) -> list[dict[str, int | float | str]]`:
     - Accepts a `COCOeval` instance on which `evaluate()` and `accumulate()` have already been called.
     - Pulls `precision = coco_eval.eval["precision"]` (shape `[T, R, K, A, M]`).
     - For each k in `range(precision.shape[2])`: compute `ap_50_95 = float(precision[:, :, k, 0, 2][precision[:, :, k, 0, 2] > -1].mean())` (mask out -1 sentinels per pycocotools convention; if mask is empty, ap_50_95 = 0.0); compute `ap_50 = float(precision[0, :, k, 0, 2][precision[0, :, k, 0, 2] > -1].mean())` with same empty-mask guard.
     - Resolve `cat_id = coco_eval.params.catIds[k]` (COCO-91 native ID).
     - Resolve `class_name = coco.cats[cat_id]["name"]`.
     - Compute `n_gt` once: count `len(coco.getAnnIds(catIds=[cat_id], iscrowd=False))`.
     - Append `{"class_id": int(cat_id), "class_name": class_name, "ap_50_95": ap_50_95, "ap_50": ap_50, "n_gt": int(n_gt)}` to the result list.
     - Return list sorted ascending by `class_id`.
   - Function `compute_per_class_ap_from_cache(cache_path: Path, dataloader: COCODataLoader) -> list[dict[str, int | float | str]]`:
     - Loads `coco_results = json.loads(cache_path.read_text(encoding="utf-8"))`.
     - Re-runs `coco_dt = dataloader.coco.loadRes(coco_results); ce = COCOeval(dataloader.coco, coco_dt, "bbox"); ce.evaluate(); ce.accumulate()`.
     - Delegates to `compute_per_class_ap_from_results(ce, dataloader.coco)`.

4) **Extend `src/benchmark/engines/base.py::evaluate_accuracy`**: After the existing `if not coco_results: ... return {...}` early-exit and BEFORE `coco_dt = dataloader.coco.loadRes(coco_results)`, write the cache:
   - Compute `cache_dir = Path("cache/predictions"); cache_dir.mkdir(parents=True, exist_ok=True)`.
   - `cache_path = cache_dir / f"coco_dt_{self.model_name}_{self.stage}.json"` — **note:** `self.stage` does NOT exist on `BaseEngine` today; the stage string is passed only to `run_full_benchmark(stage=...)`. To avoid a constructor change, accept the stage as an optional `cache_stage: str | None` parameter on `evaluate_accuracy` (default `None`) and have `run_full_benchmark` pass `cache_stage=stage`. If `cache_stage is None` OR `cache_predictions is False`, skip cache write.
   - `cache_path.write_text(json.dumps(coco_results, indent=2, ensure_ascii=False), encoding="utf-8")`.
   - After `coco_eval.accumulate()` and BEFORE `coco_eval.summarize()` (summarize prints to stdout; ordering does not matter mathematically, but call summarize last to preserve current log output), call `per_class = compute_per_class_ap_from_results(coco_eval, dataloader.coco)`.
   - **API-additive contract (LOCKED — no breaking change):** `evaluate_accuracy` returns the SAME `dict` it always has — the existing 12-key mAP/AR contract is preserved. Add ONE new key `"per_class_ap"` whose value is `list[dict[str, int | float | str]]` (length 80, sorted ascending by `class_id`). The return type annotation becomes `dict[str, float | list[dict[str, int | float | str]]]`. The caller in `run_full_benchmark` extracts this key (`per_class = accuracy.pop("per_class_ap")` or `per_class = accuracy.get("per_class_ap", [])`) and passes it to `BenchmarkResult(per_class_ap=per_class, ...)`. **Acceptance criterion: NO caller of `evaluate_accuracy` receives a tuple; the existing 12-key dict contract is preserved and exactly one key is added.**
   - Update the signature to: `evaluate_accuracy(self, dataloader: COCODataLoader, cache_stage: str | None = None, cache_predictions: bool = True) -> dict[str, float | list[dict[str, int | float | str]]]`.
   - Update `run_full_benchmark(..., cache_predictions: bool = True)` to pass both `cache_stage=stage` and `cache_predictions=cache_predictions` to `evaluate_accuracy`, then extract `per_class_ap` from the returned dict before constructing `BenchmarkResult`.
   - `import json` and `from pathlib import Path` must be moved out of TYPE_CHECKING (json is already runtime; Path is in TYPE_CHECKING — move to runtime imports).
   - `from benchmark.eval.per_class import compute_per_class_ap_from_results` — add to runtime imports.

5) **Update `.gitignore`**: Append a new section after the `# Cache` block:
   ```
   # Phase 13 per-class AP / prediction cache
   cache/
   ```

CRITICAL constraints:
- Determinism: per_class_ap entries MUST be sorted by `class_id` ascending. JSON dumps inside the cache MUST use `indent=2`, `ensure_ascii=False`, `sort_keys=False` (the list itself is sorted by class_id; key order inside each entry is preserved by Python ≥3.7 dict insertion).
- No new dependencies. Only stdlib + numpy + pycocotools + existing imports.
- All new code: `from __future__ import annotations`, module-level logger, full type annotations, ruff strict-clean. No comments unless the WHY is non-obvious (e.g. the `> -1` mask for pycocotools sentinels deserves a one-line comment).
- Do NOT alter `WARMUP_RUNS`, `MEASURE_RUNS`, or any latency/timing logic.
- Do NOT change the existing 12-key contract of `evaluate_accuracy`'s first return value.
  </action>
  <verify>
    <automated>uv run ruff check src/benchmark/utils/logger.py src/benchmark/engines/base.py src/benchmark/eval/ &amp;&amp; uv run python -c "from benchmark.eval.per_class import compute_per_class_ap_from_results, compute_per_class_ap_from_cache, PerClassAPEntry; from benchmark.utils.logger import BenchmarkResult; from dataclasses import fields; assert 'per_class_ap' in [f.name for f in fields(BenchmarkResult)]; print('OK')"</automated>
  </verify>
  <done>
    - `src/benchmark/eval/__init__.py` and `src/benchmark/eval/per_class.py` exist and pass `ruff check`.
    - `BenchmarkResult` has field `per_class_ap: list[dict[str, int | float | str]]` with `default_factory=list`.
    - `BaseEngine.evaluate_accuracy(dataloader, cache_stage=None, cache_predictions=True)` returns the SAME `dict[str, float | list[...]]` with the existing 12 keys plus one NEW key `"per_class_ap"`. No tuple, no breaking change for any external caller.
    - `BaseEngine.run_full_benchmark(..., cache_predictions: bool = True)` extracts `per_class_ap` from the returned dict and passes it into `BenchmarkResult(...)`.
    - `ResultLogger._append_csv` and `save_stage_files` strip `per_class_ap` from the CSV row dict.
    - `.gitignore` contains `cache/`.
    - Importing `benchmark.eval.per_class` succeeds and ruff strict-clean on all modified files.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build scripts/build_per_class_ap.py and backfill the 35 valid stage JSONs</name>
  <files>scripts/build_per_class_ap.py, results/rt-detr/quant/*.json, results/yolo11l/quant/*.json, results/yolo26l/quant/*.json, results/rfdetr-l/rfdetr_v1/*.json</files>
  <read_first>
    - `.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13-CONTEXT.md` (configuration scope — 35 valid configs; RF-DETR-L exclusion list)
    - `src/benchmark/cli.py` (MODEL_REGISTRY, STAGE_REGISTRY, typer patterns — mirror this style)
    - `src/benchmark/eval/per_class.py` (just written in Task 1 — the function this script calls)
    - `src/benchmark/engines/pytorch_engine.py` (PyTorch baseline pattern)
    - The four existing 1_pytorch_fp32.json files under `results/<model>/<variant>/` (sample target format)
  </read_first>
  <action>
Create `scripts/build_per_class_ap.py` — a standalone typer CLI that backfills `per_class_ap` into the 35 valid existing stage JSONs.

**Design decision (locked in this plan):** This script does NOT re-run inference. Inference is expensive (40+ minutes per config × 35 configs = 24+ hours). Instead:

Mode A (preferred, default) — `pure-postprocess`:
- For each of the 35 valid `(model, stage)` configurations: locate `cache/predictions/coco_dt_<model>_<stage>.json`.
- If the cache file does NOT exist, log a warning and skip that config (the user must run the live pipeline once to populate the cache, OR the next phase plan will provide a separate "live re-run" script — see Mode B below).
- If the cache file exists: call `compute_per_class_ap_from_cache(cache_path, dataloader)` → `per_class_ap` list of 80 entries.
- Read the existing `results/<model>/<variant>/<stage>.json`, set `data["per_class_ap"] = per_class_ap`, write back with `json.dumps(data, indent=2, ensure_ascii=False)` (preserving key insertion order — Python's json module preserves dict order).

Mode B — `live-eval` (opt-in via `--live`):
- Re-runs `evaluate_accuracy(dataloader, cache_stage=stage)` against the on-disk engine. This populates the cache AND extracts per_class_ap in one pass.
- The script must reuse the existing engine constructors (PyTorchEngine, OnnxRuntimeEngine, TensorRTEngine) via the same `MODEL_REGISTRY` and stage-id dispatch logic that `src/benchmark/cli.py::run` uses.
- For TRT stages: the script must point at the existing engine files under `engines/<model>/<engine_filename>.engine`. Construct the engine paths exactly as the existing CLI does — DO NOT hardcode paths; import or replicate the resolver helper from cli.py if one exists, otherwise use the same naming convention you observe in `engines/`.

Implementation requirements:
- Typer CLI signature:
  ```
  app = typer.Typer(...)
  @app.command()
  def main(
      models: list[str] = typer.Option(["rt-detr", "yolo11l", "yolo26l", "rfdetr-l"], "--model", "-m"),
      stages: list[str] | None = typer.Option(None, "--stage", "-s"),
      results_root: Path = typer.Option(Path("results"), "--results-root"),
      cache_root: Path = typer.Option(Path("cache/predictions"), "--cache-root"),
      live: bool = typer.Option(False, "--live", help="Re-run evaluate_accuracy against on-disk engines"),
      limit: int | None = typer.Option(None, "--limit", help="COCO image limit (None = all 5000)"),
  ) -> None: ...
  ```
- Hardcoded variant-directory mapping at module level (DO NOT auto-discover; explicit is deterministic):
  ```
  VARIANT_DIRS: dict[str, str] = {
      "rt-detr": "quant",
      "yolo11l": "quant",
      "yolo26l": "quant",
      "rfdetr-l": "rfdetr_v1",
  }
  ```
- Hardcoded exclusion list at module level:
  ```
  RFDETR_L_INVALID_STAGES: frozenset[str] = frozenset({
      "5_trt_int8_entropy",
      "5_trt_int8_minmax",
      "5_trt_int8_percentile",
      "6_trt_mixed_a",
      "6_trt_mixed_b",
  })
  ```
- Determinism: `np.random.seed(0)` and `random.seed(0)` at module load (even though the script does not use RNG, this enforces the project-wide convention).
- The iterated `(model, stage)` order is deterministic: iterate `models` in CLI argument order, iterate `STAGE_REGISTRY` from `benchmark.cli` in registry order. Skip pair `(rfdetr-l, stage)` if `stage in RFDETR_L_INVALID_STAGES`.
- Print summary at end: counts of updated/skipped/missing-cache.
- Module-level logger; `T201` ignored so `typer.echo()` and `print()` are allowed.

Special CRITICAL handling for `2_onnx_fp32` cache and `1_pytorch_fp32` cache: these stages' per_class_ap field MUST also be backfilled. If Mode A finds no cache for these stages, Mode A simply skips them with a clear log message. The expected user workflow: run `scripts/build_per_class_ap.py --live --limit 5000` ONCE end-to-end (slow but only once); subsequent reruns are Mode A (fast, deterministic, from cache).

Add a top-of-file docstring describing both modes and the expected workflow.
  </action>
  <verify>
    <automated>uv run ruff check scripts/build_per_class_ap.py &amp;&amp; uv run python scripts/build_per_class_ap.py --help</automated>
  </verify>
  <done>
    - `scripts/build_per_class_ap.py` exists, `ruff check` clean.
    - `--help` shows the documented signature.
    - When invoked in Mode A (no `--live`) and the cache is empty, the script reports 35 "missing cache" skips and exits 0 without modifying any JSON.
    - When `cache/predictions/coco_dt_<model>_<stage>.json` exists, the script rewrites the corresponding `results/<model>/<variant>/<stage>.json` adding a `per_class_ap` array of length 80 with the documented schema.
    - Rerunning the script with the same cache produces byte-identical stage JSON output (verify by sha256 of the stage JSON before/after a no-op re-run).
    - RF-DETR-L's 5 INT8/Mixed stages are NEVER touched, even when present on disk.
  </done>
</task>

</tasks>

## Behavior change to existing pipeline

After this plan ships, every `benchmark run` invocation in the project will write
`cache/predictions/coco_dt_<model>_<stage>.json` as a side effect of `evaluate_accuracy()`.
This is intentional and lets all downstream plans (P02/P03/P04) operate on cached
predictions without re-running inference. `cache/` is in `.gitignore`. To opt out,
pass `cache_predictions=False` to `run_full_benchmark` (the parameter defaults to
`True`).

<verification>
**Plan-level checks** (run after both tasks complete):

1. Ruff: `uv run ruff check src/benchmark/eval/ src/benchmark/utils/logger.py src/benchmark/engines/base.py scripts/build_per_class_ap.py` → clean.

2. Import smoke: `uv run python -c "from benchmark.eval.per_class import compute_per_class_ap_from_results, compute_per_class_ap_from_cache, PerClassAPEntry; from benchmark.utils.logger import BenchmarkResult; from dataclasses import fields; assert any(f.name == 'per_class_ap' for f in fields(BenchmarkResult))"` → exit 0.

3. CLI smoke: `uv run python scripts/build_per_class_ap.py --help` → exits 0.

4. (After the user runs Mode B at least once end-to-end) per-config check: each of the 35 valid stage JSONs contains `per_class_ap` of length 80; each entry has the documented 5 keys.

5. (Determinism, after Mode A) `sha256sum results/<model>/<variant>/<stage>.json` is stable across reruns of `scripts/build_per_class_ap.py` (no live mode).

6. Backwards compatibility: existing CSV files under `results/<model>/<variant>/<stage>.csv` are NOT regenerated by this script. The CSV writer's exclusion of `per_class_ap` is exercised the next time `ResultLogger.save_stage_files()` runs (no test required in this plan — covered by downstream live runs).
</verification>

<success_criteria>
- New `src/benchmark/eval/` package exists with `__init__.py` and `per_class.py`.
- `BenchmarkResult.per_class_ap` field exists with `default_factory=list`.
- `BaseEngine.evaluate_accuracy` accepts `cache_stage` + `cache_predictions` and returns the SAME `dict` (existing 12 keys preserved) with one new key `"per_class_ap"` (no tuple, no breaking change).
- `BaseEngine.run_full_benchmark(..., cache_predictions=True)` populates `per_class_ap` on the returned `BenchmarkResult`.
- `ResultLogger` strips `per_class_ap` from CSV writes.
- `scripts/build_per_class_ap.py` exists with Mode A / Mode B as documented.
- `.gitignore` excludes `cache/`.
- All modified and new files pass ruff strict-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13.01-SUMMARY.md` summarizing: files added, files modified, the precision-array indexing convention used, and the expected user-side workflow to backfill the 35 JSONs (Mode B once, then Mode A for re-builds).
</output>
