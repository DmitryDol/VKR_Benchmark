---
phase: 03-tensorrt-tf32-fp16-bf16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/benchmark/utils/logger.py
  - src/benchmark/engines/tensorrt_engine.py
autonomous: true
requirements:
  - TRT-01
  - TRT-02
  - TRT-03
  - TRT-04
  - TRT-05

must_haves:
  truths:
    - "TensorRTEngine builds a TF32 .engine file from the RT-DETR ONNX model within 2 GB workspace"
    - "TensorRTEngine builds a FP16 .engine file using BuilderFlag.FP16"
    - "TensorRTEngine checks platform_has_fast_native_fp16 before attempting BF16; skips with logged warning if False"
    - "All three precision builds enforce config.set_memory_pool_limit(WORKSPACE, 2 << 30)"
    - "Cached .engine file is reused on second run; --force-rebuild overrides via force_rebuild=True"
    - "BenchmarkResult carries skipped_reason field; BF16 skip populates it instead of raising"
    - "preprocess/infer/postprocess produce Detection outputs identical in format to OnnxRuntimeEngine"
  artifacts:
    - path: "src/benchmark/engines/tensorrt_engine.py"
      provides: "TensorRTEngine class with lazy build + all three precision modes"
      exports: ["TensorRTEngine"]
    - path: "src/benchmark/utils/logger.py"
      provides: "BenchmarkResult with skipped_reason field"
      contains: "skipped_reason: str = \"\""
  key_links:
    - from: "TensorRTEngine._build_engine"
      to: "tensorrt.Builder / INetworkDefinition"
      via: "trt.Builder(trt.Logger(trt.Logger.WARNING))"
      pattern: "set_memory_pool_limit.*WORKSPACE.*2 << 30"
    - from: "TensorRTEngine.infer"
      to: "IExecutionContext"
      via: "context.execute_v2 / set_tensor_address"
      pattern: "execute_v2|set_tensor_address"
    - from: "TensorRTEngine._build_engine (bf16 branch)"
      to: "builder.platform_has_fast_native_fp16"
      via: "conditional check before BuilderFlag.BF16"
      pattern: "platform_has_fast_native_fp16"
---

<objective>
Add `skipped_reason` field to BenchmarkResult and implement the full TensorRTEngine class
with lazy engine caching, three precision modes (TF32, FP16, BF16), and the complete
BaseEngine hook set (load_model, preprocess, infer, postprocess, model_size_mb).

Purpose: Delivers the TRT execution core — the engine builder, serialization/deserialization,
CUDA memory management, and inference pipeline — as a standalone module that the CLI
wires in Plan 02.

Output:
- src/benchmark/utils/logger.py — BenchmarkResult gains skipped_reason: str = ""
- src/benchmark/engines/tensorrt_engine.py — TensorRTEngine class (~180-220 lines)
</objective>

<execution_context>
@c:/projects/ResearchSSAU/VKR_Claude/.planning/phases/03-tensorrt-tf32-fp16-bf16/03-CONTEXT.md
</execution_context>

<context>
@c:/projects/ResearchSSAU/VKR_Claude/.planning/ROADMAP.md
@c:/projects/ResearchSSAU/VKR_Claude/.planning/REQUIREMENTS.md

@c:/projects/ResearchSSAU/VKR_Claude/src/benchmark/engines/base.py
@c:/projects/ResearchSSAU/VKR_Claude/src/benchmark/engines/onnx_engine.py
@c:/projects/ResearchSSAU/VKR_Claude/src/benchmark/utils/logger.py

<interfaces>
<!-- Key contracts the executor must satisfy. -->

From src/benchmark/engines/base.py:
```python
class BaseEngine(ABC):
    def __init__(self, model_name: str, engine_type: str, precision: str) -> None: ...
    def load_model(self, weights_path: Path) -> None: ...          # abstract
    def preprocess(self, sample: COCOSample) -> object: ...        # abstract
    def infer(self, inputs: object) -> object: ...                 # abstract
    def postprocess(self, raw_outputs: object, sample: COCOSample) -> Detection: ...  # abstract
    @property
    def model_size_mb(self) -> float: ...                          # abstract
    def run_full_benchmark(...) -> BenchmarkResult: ...            # concrete, calls hooks
    def reset_vram_tracking() -> None: ...                         # static
    def measure_vram(self) -> float: ...                           # concrete

WARMUP_RUNS: int = 50
MEASURE_RUNS: int = 1000
```

From src/benchmark/utils/logger.py (current BenchmarkResult tail):
```python
@dataclass
class BenchmarkResult:
    ...
    hw_trt_version: str = ""
    timestamp: str = ""
    warmup_runs: int = 50
    measure_runs: int = 1000
    # ADD after measure_runs:
    # skipped_reason: str = ""
```

From src/benchmark/engines/onnx_engine.py (preprocess + postprocess to replicate):
```python
def preprocess(self, sample: COCOSample) -> np.ndarray:
    h, w = self._input_size   # (640, 640)
    img = Image.fromarray(sample.image).resize((w, h), Image.BILINEAR)
    arr = np.array(img, dtype=np.float32) / 255.0  # HWC [0,1]
    arr = arr.transpose(2, 0, 1)                   # CHW
    return arr[np.newaxis, ...]                     # (1, 3, H, W)

def postprocess(self, raw_outputs, sample):
    logits  = raw_outputs[0][0]   # (num_queries, num_classes)
    pred_boxes = raw_outputs[1][0] # (num_queries, 4) cx cy w h norm
    # softmax → scores/labels → filter → cx cy w h → x1y1x2y2 pixel space
    # labels remapped via COCO_80_TO_91
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add skipped_reason to BenchmarkResult</name>
  <files>src/benchmark/utils/logger.py</files>
  <behavior>
    - skipped_reason field exists on BenchmarkResult with default ""
    - Field appears in asdict() output (dataclass field, not __post_init__ computed)
    - CSV header from DictWriter includes skipped_reason column
    - Normal runs have skipped_reason == ""
    - BF16 skip sets skipped_reason to non-empty string
  </behavior>
  <action>
    Add `skipped_reason: str = ""` to BenchmarkResult dataclass in
    src/benchmark/utils/logger.py. Place it after `measure_runs: int = 1000`
    (last field in the dataclass) so it does not break positional construction of
    existing callers that use keyword arguments.

    No other changes to logger.py are needed. The field becomes a CSV column
    automatically via `asdict()`.
  </action>
  <verify>
    <automated>cd c:/projects/ResearchSSAU/VKR_Claude && uv run python -c "
from benchmark.utils.logger import BenchmarkResult
import dataclasses
fields = {f.name for f in dataclasses.fields(BenchmarkResult)}
assert 'skipped_reason' in fields, 'skipped_reason field missing'
r = BenchmarkResult.__new__(BenchmarkResult)
# Check default
import dataclasses as dc
defaults = {f.name: f.default for f in dc.fields(BenchmarkResult) if f.default is not dc.MISSING}
assert defaults.get('skipped_reason') == '', f'default not empty string: {defaults.get(\"skipped_reason\")}'
print('OK: skipped_reason field present with default empty string')
"
</automated>
  </verify>
  <done>BenchmarkResult has skipped_reason: str = "" field; uv run python inline check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TensorRTEngine</name>
  <files>src/benchmark/engines/tensorrt_engine.py</files>
  <behavior>
    - TensorRTEngine(precision='tf32', engine_dir=Path('engines'), force_rebuild=False) constructs without error (TRT import guarded)
    - load_model(onnx_path) builds engine if .engine file absent, loads serialized engine if present
    - force_rebuild=True causes rebuild even when .engine file exists
    - _build_engine sets config.set_memory_pool_limit(trt.MemoryPoolType.WORKSPACE, 2 << 30) for all precisions
    - TF32 build: config.set_flag(trt.BuilderFlag.TF32)
    - FP16 build: config.set_flag(trt.BuilderFlag.FP16)
    - BF16 build: checks builder.platform_has_fast_native_fp16; if False → logs warning, raises _BF16UnsupportedError
    - load_model catches _BF16UnsupportedError → sets self._skipped_reason, returns without raising
    - model_size_mb returns .engine file size in MB (0.0 if not built)
    - preprocess matches OnnxRuntimeEngine exactly: PIL resize 640×640, /255, CHW, (1,3,H,W) float32 numpy
    - infer copies input to GPU binding, calls context.execute_v2, copies output back to CPU
    - postprocess parses logits + pred_boxes exactly as OnnxRuntimeEngine.postprocess
    - run_full_benchmark on a skipped engine returns BenchmarkResult with skipped_reason populated and all numeric metrics as float('nan')
  </behavior>
  <action>
    Create src/benchmark/engines/tensorrt_engine.py. Follow all conventions from CLAUDE.md
    (from __future__ import annotations, module docstring, module-level logger, TYPE_CHECKING
    guard, snake_case files, PascalCase classes, no print(), ruff-clean).

    Structure:

    1. Module docstring + imports.
       Guard the `import tensorrt as trt` inside a try/except ImportError that raises
       a clear RuntimeError("tensorrt package not installed ...") if missing.
       Do NOT import tensorrt at module level unconditionally — it is an optional dep.
       Import numpy as np, PIL.Image, torch; import from benchmark.data.coco_loader
       (COCO_80_TO_91) and benchmark.engines.base (BaseEngine, Detection).

    2. Private sentinel exception class:
       `class _BF16UnsupportedError(Exception): pass`

    3. Module-level constant:
       `_INPUT_SIZE: tuple[int, int] = (640, 640)`

    4. TensorRTEngine class — subclasses BaseEngine:

       ```python
       class TensorRTEngine(BaseEngine):
           def __init__(
               self,
               model_name: str,
               precision: Literal['tf32', 'fp16', 'bf16'],
               engine_dir: Path,
               force_rebuild: bool = False,
               score_threshold: float = 0.01,
           ) -> None:
               super().__init__(model_name, engine_type="tensorrt", precision=precision)
               self._engine_dir = engine_dir
               self._force_rebuild = force_rebuild
               self._score_threshold = score_threshold
               self._engine_path = engine_dir / f"rtdetr_{precision}.engine"
               self._runtime: trt.Runtime | None = None
               self._engine: trt.ICudaEngine | None = None
               self._context: trt.IExecutionContext | None = None
               self._skipped_reason: str = ""
       ```

    5. `load_model(self, weights_path: Path) -> None` — weights_path is the ONNX path.
       - Create engine_dir if not exists.
       - If not force_rebuild and _engine_path.exists(): deserialize (call _load_engine).
       - Else: try _build_engine(weights_path); catch _BF16UnsupportedError → set
         self._skipped_reason = str(exc), log warning, return.
       - After build, call _load_engine.

    6. `_build_engine(self, onnx_path: Path) -> None`:
       - Import trt inside method body (already imported at class-level guard — use the
         module-level `trt` reference).
       - Create trt.Logger(trt.Logger.WARNING), trt.Builder(logger),
         network = builder.create_network(
             1 << int(trt.NetworkDefinitionCreationFlag.EXPLICIT_BATCH)
         ).
       - Parse ONNX: parser = trt.OnnxParser(network, logger); parse onnx bytes.
       - config = builder.create_builder_config().
       - config.set_memory_pool_limit(trt.MemoryPoolType.WORKSPACE, 2 << 30).
       - Precision flags (per D-10 + CONTEXT.md):
           if self.precision == "tf32":
               config.set_flag(trt.BuilderFlag.TF32)
           elif self.precision == "fp16":
               config.set_flag(trt.BuilderFlag.FP16)
           elif self.precision == "bf16":
               if not builder.platform_has_fast_native_fp16:
                   raise _BF16UnsupportedError("BF16 not supported on this GPU")
               config.set_flag(trt.BuilderFlag.BF16)
       - serialized = builder.build_serialized_network(network, config).
       - Write serialized bytes to self._engine_path.
       - Log: logger.info("TRT %s engine built and saved: %s", self.precision, self._engine_path)

    7. `_load_engine(self) -> None`:
       - Read bytes from self._engine_path.
       - self._runtime = trt.Runtime(trt.Logger(trt.Logger.WARNING))
       - self._engine = self._runtime.deserialize_cuda_engine(data)
       - self._context = self._engine.create_execution_context()
       - Log: logger.info("TRT engine loaded: %s (%.1f MB)", ...)

    8. `preprocess` — identical to OnnxRuntimeEngine.preprocess:
       PIL resize (640×640, BILINEAR), /255.0, transpose CHW, np.newaxis → (1,3,H,W) float32.

    9. `infer(self, inputs: object) -> object`:
       - Guard: if self._context is None: check self._skipped_reason → return [] (skipped case)
       - Cast inputs to np.ndarray.
       - Allocate GPU memory for input and each output binding via cuda (use
         `import cuda.cudart as cudart` or torch CUDA allocation — prefer
         `torch.cuda` based allocation: pin input via np array, use
         `torch.as_tensor(inputs).cuda()` for input transfer).
       - For TRT 10.x API: iterate engine bindings using
         `self._engine.num_io_tensors` and `self._engine.get_tensor_name(i)`.
         For each tensor: check `self._engine.get_tensor_mode(name)` for INPUT/OUTPUT.
       - Allocate output buffers as numpy arrays with shapes from
         `self._engine.get_tensor_shape(name)` and torch dtype from
         `self._engine.get_tensor_dtype(name)`.
       - Use `self._context.set_tensor_address(name, ptr)` to bind addresses.
       - Call `self._context.execute_v2(bindings=[])` — NOTE: in TRT 10 with the
         set_tensor_address API, pass an empty list `[]` to execute_v2 (bindings param
         unused when addresses are set via set_tensor_address).
       - Copy output tensors back to CPU numpy arrays.
       - Return list of numpy arrays [logits_np, pred_boxes_np] matching OnnxRuntimeEngine
         output format so postprocess can be shared unchanged.

       IMPLEMENTATION NOTE for tensor address binding (TRT 10.x Python API):
       Use torch tensors for GPU allocation to avoid manual cuda-python calls:
       ```python
       input_gpu = torch.as_tensor(inputs_np, device="cuda")
       self._context.set_tensor_address(input_name, input_gpu.data_ptr())
       # For outputs:
       out_gpu = torch.empty(shape, dtype=torch_dtype, device="cuda")
       self._context.set_tensor_address(out_name, out_gpu.data_ptr())
       self._context.execute_v2([])
       outputs_cpu = [t.cpu().numpy() for t in output_tensors]
       ```

    10. `postprocess` — identical to OnnxRuntimeEngine.postprocess (copy the full
        implementation). Uses COCO_80_TO_91 mapping, filters by score_threshold,
        converts cx/cy/w/h normalized → x1y1x2y2 pixel space.

    11. `model_size_mb` property:
        if not self._engine_path.exists(): return 0.0
        return self._engine_path.stat().st_size / (1024 * 1024)

    12. Override `run_full_benchmark` to handle skipped case:
        If self._skipped_reason is non-empty, return a BenchmarkResult with:
        - stage=stage arg, model_name=self.model_name, engine_type="tensorrt",
          precision=self.precision
        - All latency/accuracy float fields set to float("nan")
        - skipped_reason=self._skipped_reason
        - model_size_mb=0.0, vram_peak_mb=0.0
        - warmup_runs=WARMUP_RUNS, measure_runs=MEASURE_RUNS
        Otherwise: call super().run_full_benchmark(...) normally.

        Import WARMUP_RUNS, MEASURE_RUNS from benchmark.engines.base.

    RUFF COMPLIANCE:
    - Use `from __future__ import annotations` at top.
    - All params and returns annotated.
    - Use `TYPE_CHECKING` block for COCOSample, COCODataLoader (only used in type hints).
    - No bare `except:` — catch specific exception types.
    - Use `msg = ...; raise ExcType(msg)` pattern for raised errors.
    - No T201 (no print statements).
  </action>
  <verify>
    <automated>cd c:/projects/ResearchSSAU/VKR_Claude && uv run ruff check src/benchmark/engines/tensorrt_engine.py && uv run python -c "
from benchmark.engines.tensorrt_engine import TensorRTEngine
from pathlib import Path
# Instantiation should succeed (TRT may not be installed in CI — guard test)
try:
    e = TensorRTEngine(model_name='rt-detr', precision='tf32', engine_dir=Path('engines'))
    print('OK: TensorRTEngine instantiated, precision=tf32')
    e2 = TensorRTEngine(model_name='rt-detr', precision='bf16', engine_dir=Path('engines'))
    print('OK: TensorRTEngine instantiated, precision=bf16')
except RuntimeError as ex:
    if 'tensorrt' in str(ex).lower():
        print('OK: TensorRT not installed but import guard works correctly')
    else:
        raise
"
</automated>
  </verify>
  <done>
    tensorrt_engine.py passes ruff check with zero errors. TensorRTEngine can be
    imported and instantiated (import guard fires cleanly if TRT not installed).
    All three precision branches present. BF16 hardware check present.
    2 GB workspace limit present in _build_engine.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → engine_dir | Path from user CLI flags used to read/write .engine files |
| ONNX file → TRT parser | Serialized model bytes parsed by TensorRT OnnxParser |
| TRT engine bytes → deserializer | Engine file read from disk and deserialized to CUDA memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | .engine cache file | accept | Engine files are local research artifacts; no integrity check needed for academic pipeline. User controls the engines/ dir. |
| T-03-02 | Tampering | ONNX file input to TRT parser | accept | ONNX file is produced by project's own onnx_export.py in prior phase; not user-supplied from untrusted source. |
| T-03-03 | Denial of Service | TRT build OOM | mitigate | 2 GB workspace cap via set_memory_pool_limit enforced in _build_engine for all precision branches. |
| T-03-04 | Information Disclosure | TRT Logger output | accept | trt.Logger.WARNING level suppresses verbose info; no secrets in model graph. |
| T-03-05 | Elevation of Privilege | engine_dir path traversal | mitigate | CLI resolves output_dir via Path(...).resolve() (existing T-02-05 mitigation in cli.py); engine_dir derived from resolved path. |
</threat_model>

<verification>
After both tasks complete:

```bash
cd c:/projects/ResearchSSAU/VKR_Claude
uv run ruff check src/benchmark/engines/tensorrt_engine.py src/benchmark/utils/logger.py
uv run python -c "
import dataclasses
from benchmark.utils.logger import BenchmarkResult
from benchmark.engines.tensorrt_engine import TensorRTEngine
fields = {f.name for f in dataclasses.fields(BenchmarkResult)}
assert 'skipped_reason' in fields
print('BenchmarkResult.skipped_reason OK')
from pathlib import Path
try:
    e = TensorRTEngine('rt-detr', 'fp16', Path('engines'))
    print('TensorRTEngine OK')
except RuntimeError as ex:
    if 'tensorrt' in str(ex).lower(): print('TRT guard OK')
    else: raise
"
```
</verification>

<success_criteria>
1. ruff check passes with zero errors on both modified files
2. BenchmarkResult has skipped_reason: str = "" field visible in asdict() output
3. TensorRTEngine class is importable; instantiation with precision in {'tf32','fp16','bf16'} works
4. _build_engine contains set_memory_pool_limit(WORKSPACE, 2 << 30) for all precision branches
5. BF16 branch checks builder.platform_has_fast_native_fp16 before setting BuilderFlag.BF16
6. run_full_benchmark override returns NaN-filled BenchmarkResult when _skipped_reason is set
</success_criteria>

<output>
After completion, create `.planning/phases/03-tensorrt-tf32-fp16-bf16/03-01-SUMMARY.md`
following the standard summary template.
</output>
