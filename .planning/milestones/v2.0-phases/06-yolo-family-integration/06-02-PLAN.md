---
phase: 06-yolo-family-integration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [scripts/download_weights.py, src/benchmark/models/yolo_adapter.py, src/benchmark/models/__init__.py, tests/test_yolo_adapters.py]
autonomous: true
requirements: [ADPT-04, ADPT-08]
must_haves:
  truths:
    - "YOLO11l and YOLO26l weights are present in weights/"
    - "YOLOAdapter passes unit tests for NMS and NMS-free parsing"
  artifacts:
    - path: "src/benchmark/models/yolo_adapter.py"
      provides: "YOLOAdapter implementing ModelAdapter"
    - path: "tests/test_yolo_adapters.py"
      provides: "Unit tests for YOLOAdapter"
---

<objective>
Integrate YOLO11l and YOLO26l models into the benchmarking pipeline. 
Includes weight downloading, implementing the ModelAdapter for the YOLO family using the Ultralytics API, and formal unit testing of the adapters.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/milestones/v2.0-phases/06-yolo-family-integration/06-CONTEXT.md
@scripts/download_weights.py
@src/benchmark/models/__init__.py
@src/benchmark/data/coco_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update weight downloader for YOLO models</name>
  <files>scripts/download_weights.py</files>
  <action>
    Add `download_yolo_weights()` function to `scripts/download_weights.py`:
    1. Import `YOLO` from `ultralytics`.
    2. Add logic to download `yolo11l.pt` and `yolo26l.pt`.
    3. Ensure weights are stored in the `weights/` directory.
  </action>
  <verify>
    <automated>uv run python scripts/download_weights.py</automated>
  </verify>
  <done>YOLO11l and YOLO26l weights are downloaded and ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement YOLOAdapter</name>
  <files>src/benchmark/models/yolo_adapter.py</files>
  <behavior>
    - `YOLOAdapter` must implement `ModelAdapter` protocol.
    - `infer()` must pass inputs to the model.
    - `parse_outputs()` must handle NMS-free (YOLO26) and NMS (YOLO11) outputs correctly.
    - Resulting detections must use COCO-91 category IDs.
  </behavior>
  <action>
    Create `src/benchmark/models/yolo_adapter.py`:
    1. Implement `YOLOAdapter` conforming to `ModelAdapter`.
    2. Use `ultralytics.YOLO` for loading.
    3. Implement `infer()` to pass inputs directly to the model (positional).
    4. Implement `parse_outputs()`:
       - For YOLO26, use the default end2end (NMS-free) output.
       - For YOLO11, use Ultralytics built-in NMS (via `ops.non_max_suppression`).
    5. Use `COCO_80_TO_91` from `coco_loader.py` for category mapping.
  </action>
  <verify>
    <automated>ruff check src/benchmark/models/yolo_adapter.py</automated>
  </verify>
  <done>YOLOAdapter is implemented with support for YOLO11 and YOLO26 (NMS-free).</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for YOLO adapters</name>
  <files>tests/test_yolo_adapters.py</files>
  <action>
    Create `tests/test_yolo_adapters.py`:
    1. Define unit tests for `YOLOAdapter`.
    2. Mock the `ultralytics.YOLO` model output to verify `parse_outputs` logic for both YOLO11 (NMS) and YOLO26 (NMS-free) modes.
    3. Verify that the output labels are correctly mapped to COCO-91 IDs.
  </action>
  <verify>
    <automated>pytest tests/test_yolo_adapters.py</automated>
  </verify>
  <done>Formal unit tests for YOLO adapters are created and passing.</done>
</task>

<task type="auto">
  <name>Task 4: Register YOLOAdapter and Verify via script</name>
  <files>src/benchmark/models/__init__.py, scripts/verify_yolo.py</files>
  <action>
    1. Export `YOLOAdapter` in `src/benchmark/models/__init__.py`.
    2. Create `scripts/verify_yolo.py` that loads YOLO11l and YOLO26l, runs a single image through `PyTorchEngine`, and prints detection counts.
  </action>
  <verify>
    <automated>uv run python scripts/verify_yolo.py</automated>
  </verify>
  <done>YOLO models are registered and verified to produce valid detections in a real integration test.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Denial of Service | download_weights.py | accept | Network failures during download are expected; user can retry. |
| T-06-03 | Information Disclosure | weights/ | accept | Model weights are public; no PII involved. |
</threat_model>

<success_criteria>
YOLO11l and YOLO26l can be loaded and run via PyTorchEngine, producing standard Detection objects, backed by both unit tests and integration scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-yolo-family-integration/06-02-SUMMARY.md`
</output>
