---
phase: 13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion
plan: "13.07"
subsystem: docs
tags: [docs-only, screenshots, manual-capture, media]
dependency_graph:
  requires: []
  provides: [media/screenshots/README.md]
  affects: []
tech_stack:
  added: []
  patterns: []
key_files:
  created:
    - media/screenshots/README.md
  modified: []
decisions:
  - "One CLI command covers all 3 screenshots (benchmark run --model rt-detr --stage 4_trt_fp16) — simpler than 3 separate commands"
  - "PowerShell snippet uses foreach + Test-Path; bash snippet mirrors it exactly — both exit 0 when PNGs are missing"
metrics:
  duration: "< 5 min"
  completed: "2026-05-19"
  tasks_completed: 1
  tasks_total: 1
---

# Phase 13 Plan 07: Screenshots README Summary

Authored `media/screenshots/README.md` — English Markdown instructions for manually capturing the three console-output PNGs required by the diploma advisor's Task 7.

## What was built

Single documentation file `media/screenshots/README.md` (109 lines). No Python code produced.

## Section structure

Sections in required order:

1. **Title + 1-sentence intro** — identifies the 3 screenshots as manual diploma-defense artifacts.
2. **`## Expected files`** — bullet list of `trt_build_log.png`, `benchmark_output.png`, `pycocotools_output.png` with one-line descriptions of the required visible content.
3. **`## How to capture`** — three numbered sub-sections, each providing: exact PowerShell command (`uv run python -m benchmark run --model rt-detr --stage 4_trt_fp16`), what to wait for before screenshotting, terminal config (120 cols, dark bg, Cascadia Mono 12 pt), save path, and `Win + Shift + S` capture method.
4. **`## Verification`** — PowerShell `foreach` + bash `for` loop, both checking file existence with `[OK]` / `[MISSING]` output, both exit-0 when files are absent.
5. **`## Notes`** — four constraints: manual-only, final screenful only, PNG required (no JPEG), figure-number reference in diploma text.

## Verification snippet design

The PowerShell snippet uses `Test-Path -Path "media/screenshots/$f"` — runs from project root without requiring any Python or extra tools. The bash equivalent uses POSIX `[ -f ... ]`. Both return exit code 0 regardless of whether the PNGs exist, so they never block the workflow.

The automated plan verify command passed:

```
uv run python -c "... assert '## Expected files' in txt; ... print('OK')"
OK
```

## Deviations from Plan

None — plan executed exactly as written. One CLI command covers all three screenshots, which is a simplification consistent with the plan's "capture the same run" intent.

## Self-Check: PASSED

- `media/screenshots/README.md` exists: FOUND
- Commit `331a4ed` exists: FOUND
- All 4 required `##` sections present: VERIFIED (automated assert passed)
- All 3 PNG filenames present: VERIFIED
- PowerShell + bash snippets present: VERIFIED
- No Cyrillic, no emoji, no timestamps: VERIFIED
