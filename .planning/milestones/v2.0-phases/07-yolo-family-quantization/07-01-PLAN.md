---
phase: "07"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "weights/yolo11l/yolo11l_sim.onnx"
  - "weights/yolo26l/yolo26l_sim.onnx"
  - "results/results.csv"
  - "results/results.json"
autonomous: true
requirements: ["OPT-YOLO-01"]

must_haves:
  truths:
    - "Simplified ONNX models exist for both YOLO variants"
    - "PyTorch FP32 (Stage 1) and ONNX FP32 (Stage 2) benchmarks are recorded"
    - "ONNX Runtime accuracy matches PyTorch baseline accuracy"
  artifacts:
    - path: "weights/yolo11l/yolo11l_sim.onnx"
      provides: "Optimized graph for yolo11l"
    - path: "weights/yolo26l/yolo26l_sim.onnx"
      provides: "Optimized graph for yolo26l"
  key_links:
    - from: "weights/yolo11l/yolo11l_sim.onnx"
      to: "results/results.csv"
      via: "benchmark.cli run --stage 2_onnx_fp32"
---

<objective>
Establish the accuracy baseline for YOLO family models (Stage 1) and prepare optimized ONNX graphs (Stage 2) for the TensorRT pipeline.

Purpose: Create a verified baseline and input graph for all subsequent TensorRT optimization stages, ensuring data continuity using a unified run-id.
Output: Two simplified ONNX artifacts and Stage 1/2 metrics recorded in results.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/milestones/v2.0-phases/07-yolo-family-quantization/07-CONTEXT.md
@.planning/milestones/v2.0-phases/07-yolo-family-quantization/RESEARCH.md
@scripts/export_yolo_onnx.py
@src/benchmark/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export YOLO models to simplified ONNX</name>
  <files>weights/yolo11l/yolo11l_sim.onnx, weights/yolo26l/yolo26l_sim.onnx</files>
  <action>
    Run the export script: `uv run python scripts/export_yolo_onnx.py`.
    This script loads .pt weights, exports to ONNX (opset 17), simplifies with onnxsim, and validates the output.
  </action>
  <verify>
    <automated>ls weights/yolo11l/yolo11l_sim.onnx weights/yolo26l/yolo26l_sim.onnx</automated>
  </verify>
  <done>Simplified ONNX files created and validated for both models.</done>
</task>

<task type="auto">
  <name>Task 2: Benchmark Stage 1 and Stage 2</name>
  <files>results/results.csv</files>
  <action>
    Run Stage 1 (PyTorch FP32) and Stage 2 (ONNX FP32) benchmarks for both models using a consistent run-id:
    1. `uv run python -m benchmark.cli run --model yolo11l --stage 1_pytorch_fp32,2_onnx_fp32 --run-id yolo-quantization`
    2. `uv run python -m benchmark.cli run --model yolo26l --stage 1_pytorch_fp32,2_onnx_fp32 --run-id yolo-quantization`
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep -E "1_pytorch_fp32|2_onnx_fp32" | grep -c "yolo"</automated>
  </verify>
  <done>Stage 1 and 2 metrics (4 entries total) recorded in results.csv under run-id 'yolo-quantization'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PyTorch → ONNX | Weights and graph structure are serialized; corruption here invalidates all downstream stages. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Exported ONNX | mitigate | Use `onnx.checker.check_model` (embedded in script) to ensure graph integrity. |
</threat_model>

<verification>
Check `results/results.csv` for 4 new entries (yolo11l/yolo26l x Stage 1/2) with `run_id=yolo-quantization`.
</verification>

<success_criteria>
- yolo11l_sim.onnx and yolo26l_sim.onnx exist.
- 4 benchmark entries recorded in results.csv.
- Stage 2 mAP is within 0.1% of Stage 1 mAP for both models.
</success_criteria>

<output>
After completion, create `.planning/phases/07-yolo-family-quantization/07-01-SUMMARY.md`
</output>
