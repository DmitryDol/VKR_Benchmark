---
phase: 13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion
plan: 13.07
type: execute
wave: 3
depends_on: []
files_modified:
  - media/screenshots/README.md
autonomous: true
requirements: [VKR-13-T7]

must_haves:
  truths:
    - "media/screenshots/README.md exists"
    - "README documents how to capture exactly 3 PNG screenshots: trt_build_log.png, benchmark_output.png, pycocotools_output.png"
    - "README lists the exact CLI commands the user must run to produce each console output"
    - "README provides a verification command (PowerShell + bash equivalents) that checks the 3 PNGs exist at the expected paths"
    - "README content is English (advisor spec says all PNG text is English; consistency for docs as well)"
    - "Re-reading the README produces a byte-identical file (no timestamp, no $(date) interpolation)"
  artifacts:
    - path: "media/screenshots/README.md"
      provides: "Capture instructions for the 3 screenshot PNGs"
  key_links:
    - from: "media/screenshots/README.md"
      to: "scripts/build_per_class_ap.py + benchmark CLI"
      via: "documented CLI commands the user runs to produce the console output that gets screenshotted"
      pattern: "scripts/|benchmark run"
---

<objective>
Write the documentation page that tells the user how to capture the three console-output screenshots the advisor's spec requires. No Python code in this plan — pure docs + verification command. Phase 13 cannot capture screenshots autonomously (they require a human-attended terminal session), so the deliverable is the recipe + a verification command.

Purpose: The advisor's Task 7 — the diploma chapter on the optimization process needs visual evidence that the pipeline actually ran (TRT build log, benchmark summary, pycocotools COCOeval output).

Output:
- `media/screenshots/README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13-CONTEXT.md

@prompt4edits/code-agent-vkr-artifacts.md
@CLAUDE.md

@src/benchmark/cli.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author media/screenshots/README.md</name>
  <files>media/screenshots/README.md</files>
  <read_first>
    - `.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13-CONTEXT.md` (Screenshots — locked section)
    - `prompt4edits/code-agent-vkr-artifacts.md` (advisor spec — Task 7 — to extract the recommended terminal config, if any)
    - `src/benchmark/cli.py` (to confirm the exact CLI invocation that emits the relevant console output)
  </read_first>
  <action>
Write `media/screenshots/README.md` as English Markdown. The file MUST contain the following sections in this order (no other sections):

1. **Heading + 1-sentence intro** explaining that this folder contains 3 manual screenshots for the diploma defense.

2. **`## Expected files`** — bullet list of the 3 expected files with one-line descriptions:
   - `trt_build_log.png` — screenshot of a TensorRT engine build (must show the `[I] Total Activation Memory`, `[I] Total Weights Memory`, and `[I] Total Host/Device Memory` log lines, plus a successful `Engine built in` line at the bottom)
   - `benchmark_output.png` — screenshot of the `benchmark run --model rt-detr --stage 4_trt_fp16` output, showing the latency/mAP summary table at the end
   - `pycocotools_output.png` — screenshot of a pycocotools COCOeval summary table (the 12-line `Average Precision (AP) @ [...] = 0.xxx` block) at the end of any `evaluate_accuracy` run

3. **`## How to capture`** — three numbered sub-sections (one per screenshot) listing:
   - The exact terminal command to run (PowerShell variant; mention that bash users can drop the `$env:` prefix and substitute `&&` if needed).
   - What console output to wait for before taking the screenshot.
   - The recommended terminal config: at least 120 columns wide, dark background, monospace font (e.g. Cascadia Mono 12 pt on Windows), DO NOT scroll the buffer (capture the bottom of the output where the summary appears).
   - The save location: `media/screenshots/<filename>.png`.
   - Use Windows `Win + Shift + S` for Snipping Tool, save as PNG, place file at the documented path.

4. **`## Verification`** — exact commands the user runs to check the 3 PNGs are present.

   PowerShell (default for this project per CLAUDE.md):
   ```powershell
   foreach ($f in @("trt_build_log.png", "benchmark_output.png", "pycocotools_output.png")) {
       if (Test-Path -Path "media/screenshots/$f") {
           Write-Host "[OK] $f"
       } else {
           Write-Host "[MISSING] $f"
       }
   }
   ```

   Bash equivalent (for any user running under WSL/Linux):
   ```bash
   for f in trt_build_log.png benchmark_output.png pycocotools_output.png; do
     if [ -f "media/screenshots/$f" ]; then echo "[OK] $f"; else echo "[MISSING] $f"; fi
   done
   ```

5. **`## Notes`** — explicit constraints:
   - All 3 screenshots are MANUAL — there is no automation for this task (the spec calls them out as user-captured deliverables).
   - Screenshots should be the FINAL screenful at the end of the relevant run — do NOT include intermediate progress bars.
   - File format MUST be PNG (no JPEG — the advisor spec specifically requests PNG).
   - The screenshots are part of the diploma chapter on optimization process. They will be referenced by figure number in the diploma text.

**Style constraints:**
- All headings use `## ` (level 2).
- Code blocks fenced with triple backticks, with the language tag set explicitly (`powershell`, `bash`, `text`).
- Plain ASCII / Latin-1 characters only — no Cyrillic.
- File ends with a single trailing newline.
- DO NOT include a date, timestamp, generated-by, or any field that varies across reruns. The file MUST be byte-identical if re-authored from the same plan.

**Forbidden content:**
- Do NOT include any `$(date)` or `{{NOW}}` placeholders.
- Do NOT include URLs that require live fetching (offline-friendly).
- Do NOT include emoji (CLAUDE.md prohibits emoji unless explicitly requested).
- Do NOT include diploma project name, repo name, or branch name — these change.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; p = Path('media/screenshots/README.md'); assert p.exists(); txt = p.read_text(encoding='utf-8'); assert 'trt_build_log.png' in txt; assert 'benchmark_output.png' in txt; assert 'pycocotools_output.png' in txt; assert '## Expected files' in txt; assert '## How to capture' in txt; assert '## Verification' in txt; assert '## Notes' in txt; print('OK')"</automated>
  </verify>
  <done>
    - `media/screenshots/README.md` exists.
    - Contains all 4 required `##` sections in the documented order.
    - Mentions all 3 PNG filenames.
    - Contains a PowerShell verification snippet AND a bash verification snippet.
    - All text is English ASCII / Latin-1 (no Cyrillic, no emoji).
    - File ends with a single trailing newline.
    - Re-running the verification snippet on a clean working tree where the 3 PNGs do not yet exist prints 3 `[MISSING]` lines without erroring.
  </done>
</task>

</tasks>

<verification>
1. File existence: `[ -f media/screenshots/README.md ]` → true.
2. Section structure: the automated `<verify>` block confirms all 4 `## ` headings and all 3 PNG filenames are present.
3. Verification command smoke test (with no PNGs captured yet):
   ```powershell
   foreach ($f in @("trt_build_log.png", "benchmark_output.png", "pycocotools_output.png")) {
       if (Test-Path -Path "media/screenshots/$f") { "[OK] $f" } else { "[MISSING] $f" }
   }
   ```
   Expected output: 3 `[MISSING]` lines. PowerShell exit code: 0.
4. Determinism: re-authoring the README via the same plan produces byte-identical content (test: write twice, sha256sum compare).
</verification>

<success_criteria>
- `media/screenshots/README.md` exists with the documented 4 sections.
- PowerShell + bash verification snippets present and syntactically valid.
- File is English-only, byte-identical on re-author.
</success_criteria>

<output>
After completion, create `.planning/phases/13-vkr-diploma-artifacts-per-class-ap-in-json-reports-confusion/13.07-SUMMARY.md` covering: the chosen section structure, the verification snippet design, and confirmation that no code was produced (docs only).
</output>
