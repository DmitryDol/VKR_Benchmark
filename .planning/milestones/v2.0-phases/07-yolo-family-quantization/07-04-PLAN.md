---
phase: "07"
plan: "04"
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - "engines/yolo11l_mixed_a.engine"
  - "engines/yolo11l_mixed_b.engine"
  - "engines/yolo26l_mixed_a.engine"
  - "engines/yolo26l_mixed_b.engine"
  - "results/results.csv"
  - "results/summary.md"
autonomous: true
requirements: ["OPT-YOLO-04", "OPT-YOLO-05"]

must_haves:
  truths:
    - "Mixed Precision engines (Strategy A and B) are built using the best-performing calibrator"
    - "YOLO family metrics are merged and summarized in a final report"
  artifacts:
    - path: "results/summary.md"
      provides: "Final phase summary with cross-stage comparisons"
  key_links:
    - from: "results/results.csv"
      to: "results/summary.md"
      via: "benchmark.cli run --stage 6_trt_mixed_*"
---

<objective>
Apply Mixed Precision strategies (INT8 + FP16) to YOLO models and perform final verification of the optimization pipeline.

Purpose: Minimize accuracy drop in YOLO11l caused by DFL sensitivity and demonstrate robust optimization for YOLO26l, producing a unified report via consistent run-id and explicit merge.
Output: Four Mixed Precision engines and a finalized performance report.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.0-phases/07-yolo-family-quantization/07-CONTEXT.md
@src/benchmark/cli.py
@src/benchmark/engines/mixed_precision.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Stage 6 for YOLO Family</name>
  <files>engines/yolo11l_mixed_a.engine, engines/yolo11l_mixed_b.engine, engines/yolo26l_mixed_a.engine, engines/yolo26l_mixed_b.engine</files>
  <action>
    Run Mixed Precision stages for both models with run-id:
    1. `uv run python -m benchmark.cli run --model yolo11l --stage 6_trt_mixed_a,6_trt_mixed_b --run-id yolo-quantization`
    2. `uv run python -m benchmark.cli run --model yolo26l --stage 6_trt_mixed_a,6_trt_mixed_b --run-id yolo-quantization`
    These stages automatically use the best calibrator identified in Plan 07-03.
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep "6_trt_mixed" | grep -c "yolo"</automated>
  </verify>
  <done>Mixed Precision benchmarks (4 entries) recorded under 'yolo-quantization'.</done>
</task>

<task type="auto">
  <name>Task 2: Merge Results and Generate Summary</name>
  <files>results/results.csv, results/summary.md</files>
  <action>
    Finalize the results by merging all stages for the yolo-quantization run:
    1. Merge results: `uv run python -m benchmark.cli merge --run-id yolo-quantization`
    2. Verify `results.csv` contains all stages (1-6) for both models.
    3. Ensure `results/summary.md` is updated with the latest metrics.
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep -c "yolo" | grep -E "^20$"</automated>
  </verify>
  <done>Phase 7 report generated. Verified 20 entries (2 models * 10 stages: 1, 2, 3, 4x2, 5x3, 6x2) in results.csv for run-id 'yolo-quantization'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Metrics → Report | Accuracy of the final report depends on the integrity of the results.csv collected across all tasks. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | Results | accept | Benchmarking metrics are non-sensitive technical data intended for publication in the diploma. |
| T-07-05 | Tampering | Summary Report | mitigate | Use incremental logging to CSV to ensure data isn't lost if a crash occurs during summary generation. |
</threat_model>

<verification>
Check `results/summary.md` for YOLO11l and YOLO26l comparison tables covering all stages.
</verification>

<success_criteria>
- All Mixed Precision engines built and benchmarked.
- 20 total entries for 'yolo-quantization' in results.csv.
- Final mAP and Latency metrics for all 6 stages (per model) are present in the final report.
</success_criteria>

<output>
After completion, create `.planning/phases/07-yolo-family-quantization/07-04-SUMMARY.md`
</output>
