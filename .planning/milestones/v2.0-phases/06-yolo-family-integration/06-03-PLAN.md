---
phase: 06-yolo-family-integration
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [scripts/run_yolo_phase.py, results/results.csv, results/results.json]
autonomous: true
requirements: [ADPT-04, ADPT-08]
must_haves:
  truths:
    - "results/results.csv contains Stage 1 entries for yolo11l and yolo26l"
    - "mAP results for YOLO family are non-zero and plausible"
  artifacts:
    - path: "results/results.csv"
      provides: "Benchmark results for YOLO family"
    - path: "scripts/run_yolo_phase.py"
      provides: "YOLO-specific Stage 1 runner"
---

<objective>
Execute Stage 1 (FP32 baseline) benchmarks for YOLO11l and YOLO26l. 
Records Latency, throughput, mAP, and VRAM usage for the newly integrated models.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/milestones/v2.0-phases/06-yolo-family-integration/06-CONTEXT.md
@scripts/run_phase1.py
@src/benchmark/engines/pytorch_engine.py
@src/benchmark/models/yolo_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YOLO phase runner script</name>
  <files>scripts/run_yolo_phase.py</files>
  <action>
    Create `scripts/run_yolo_phase.py` by adapting `scripts/run_phase1.py`:
    1. Import `YOLOAdapter`.
    2. Configure benchmark to run for `yolo11l` and `yolo26l`.
    3. Ensure Stage 1 metrics are recorded via `ResultLogger`.
    4. Support `--limit` flag for quick validation.
  </action>
  <verify>
    <automated>ruff check scripts/run_yolo_phase.py</automated>
  </verify>
  <done>YOLO phase runner is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Execute Stage 1 Benchmarks</name>
  <files>results/results.csv, results/results.json</files>
  <action>
    Run the YOLO baseline benchmark:
    `uv run python scripts/run_yolo_phase.py --limit 500`
    (Using 500 images as a representative sample for the phase verification, full run left to final validation).
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path results/results.csv -Pattern 'yolo11l', 'yolo26l'"</automated>
  </verify>
  <done>Baseline metrics for YOLO family are recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Stage 1 Benchmarks for YOLO family completed.</what-built>
  <how-to-verify>
    Inspect `results/results.csv`.
    Ensure `yolo11l` and `yolo26l` have entries with:
    - engine: pytorch
    - precision: fp32
    - stage: 1
    - mAP, Latency, FPS, VRAM recorded.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | results/results.csv | accept | Logs are for internal research; integrity is best-effort. |
</threat_model>

<success_criteria>
Baseline metrics for YOLO11l and YOLO26l are successfully persisted in the results tracking system.
</success_criteria>

<output>
After completion, create `.planning/phases/06-yolo-family-integration/06-03-SUMMARY.md`
</output>
