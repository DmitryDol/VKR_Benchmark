---
phase: "07"
plan: "03"
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - "engines/yolo11l_int8_minmax.engine"
  - "engines/yolo11l_int8_entropy.engine"
  - "engines/yolo11l_int8_percentile.engine"
  - "engines/yolo26l_int8_minmax.engine"
  - "engines/yolo26l_int8_entropy.engine"
  - "engines/yolo26l_int8_percentile.engine"
  - "results/results.csv"
autonomous: true
requirements: ["OPT-YOLO-03"]

must_haves:
  truths:
    - "INT8 calibration completes for all three methods (MinMax, Entropy, Percentile)"
    - "Benchmarks include run-id yolo-quantization for all stages"
    - "System identifies and persists the 'best' calibrator for each model"
  artifacts:
    - path: "results/yolo11l/*/int8_best_calibrator.json"
      provides: "Best calibrator selection for yolo11l"
    - path: "results/yolo26l/*/int8_best_calibrator.json"
      provides: "Best calibrator selection for yolo26l"
  key_links:
    - from: "engines/yolo11l_int8_entropy.engine"
      to: "results/results.csv"
      via: "benchmark.cli run --stage 5_trt_int8_entropy"
---

<objective>
Execute multiple INT8 calibration passes for YOLO models to determine the optimal calibration method (MinMax, Entropy, or Percentile).

Purpose: Maximize accuracy recovery in INT8 precision before applying Mixed Precision strategies, ensuring data continuity using a unified run-id.
Output: Six INT8 engines and two 'best calibrator' JSON metadata files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.0-phases/07-yolo-family-quantization/07-CONTEXT.md
@src/benchmark/cli.py
@src/benchmark/engines/int8_calibrators.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Stage 5 for YOLO11l</name>
  <files>engines/yolo11l_int8_minmax.engine, engines/yolo11l_int8_entropy.engine, engines/yolo11l_int8_percentile.engine</files>
  <action>
    Run all 3 INT8 stages for YOLO11l with run-id:
    `uv run python -m benchmark.cli run --model yolo11l --stage 5_trt_int8_minmax,5_trt_int8_entropy,5_trt_int8_percentile --run-id yolo-quantization`
    Each stage uses 500 images for calibration per 07-CONTEXT.md.
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep "yolo11l" | grep "5_trt_int8" | grep -c "yolo11l"</automated>
  </verify>
  <done>YOLO11l INT8 calibration (3 entries) recorded under 'yolo-quantization'.</done>
</task>

<task type="auto">
  <name>Task 2: Run Stage 5 for YOLO26l</name>
  <files>engines/yolo26l_int8_minmax.engine, engines/yolo26l_int8_entropy.engine, engines/yolo26l_int8_percentile.engine</files>
  <action>
    Run all 3 INT8 stages for YOLO26l with run-id:
    `uv run python -m benchmark.cli run --model yolo26l --stage 5_trt_int8_minmax,5_trt_int8_entropy,5_trt_int8_percentile --run-id yolo-quantization`
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep "yolo26l" | grep "5_trt_int8" | grep -c "yolo26l"</automated>
  </verify>
  <done>YOLO26l INT8 calibration (3 entries) recorded under 'yolo-quantization'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Dataset → Calibration | Representative data is required for accurate quantization; 500 images are used to ensure coverage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | Calibration Cache | accept | Histogram caches stored locally in `engines/` are low-risk artifacts containing statistical weights, not PII. |
</threat_model>

<verification>
Check `results/results.csv` for 6 new Stage 5 entries with `run_id=yolo-quantization`. Verify existence of `int8_best_calibrator.json` in the latest run directory.
</verification>

<success_criteria>
- All 6 INT8 engines built.
- 6 benchmark entries recorded in results.csv.
- Best calibrator selected based on highest mAP in Stage 5.
</success_criteria>

<output>
After completion, create `.planning/phases/07-yolo-family-quantization/07-03-SUMMARY.md`
</output>
