---
phase: "07"
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - "engines/yolo11l_tf32.engine"
  - "engines/yolo11l_fp16.engine"
  - "engines/yolo11l_bf16.engine"
  - "engines/yolo26l_tf32.engine"
  - "engines/yolo26l_fp16.engine"
  - "engines/yolo26l_bf16.engine"
  - "results/results.csv"
autonomous: true
requirements: ["OPT-YOLO-02"]

must_haves:
  truths:
    - "TensorRT engines are successfully built for TF32, FP16, and BF16 precisions"
    - "Benchmarks include run-id yolo-quantization for all stages"
    - "Standard precision mAP matches baseline accuracy within 0.1%"
  artifacts:
    - path: "engines/yolo11l_fp16.engine"
      provides: "FP16 optimized engine for yolo11l"
    - path: "engines/yolo26l_fp16.engine"
      provides: "FP16 optimized engine for yolo26l"
  key_links:
    - from: "weights/yolo11l/yolo11l_sim.onnx"
      to: "engines/yolo11l_tf32.engine"
      via: "TensorRT Builder"
---

<objective>
Build and benchmark TensorRT engines for YOLO models in standard hardware-accelerated precisions (TF32, FP16, BF16).

Purpose: Quantify the latency benefits of TensorRT and hardware precision support on the RTX 3070 while maintaining accuracy parity, using a consistent run-id for data continuity.
Output: Six TensorRT engines and corresponding benchmark metrics.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2.0-phases/07-yolo-family-quantization/07-CONTEXT.md
@src/benchmark/cli.py
@src/benchmark/engines/tensorrt_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark YOLO11l Stages 3-4</name>
  <files>engines/yolo11l_tf32.engine, engines/yolo11l_fp16.engine, engines/yolo11l_bf16.engine</files>
  <action>
    Run Stages 3 and 4 for YOLO11l with run-id:
    `uv run python -m benchmark.cli run --model yolo11l --stage 3_trt_tf32,4_trt_fp16,4_trt_bf16 --run-id yolo-quantization`
    This will build the engines (if not cached) and run 1000-iteration latency measurements.
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep "yolo11l" | grep -E "3_trt_tf32|4_trt_fp16|4_trt_bf16" | grep -c "yolo11l"</automated>
  </verify>
  <done>YOLO11l standard precision benchmarks (3 entries) recorded under 'yolo-quantization'.</done>
</task>

<task type="auto">
  <name>Task 2: Benchmark YOLO26l Stages 3-4</name>
  <files>engines/yolo26l_tf32.engine, engines/yolo26l_fp16.engine, engines/yolo26l_bf16.engine</files>
  <action>
    Run Stages 3 and 4 for YOLO26l with run-id:
    `uv run python -m benchmark.cli run --model yolo26l --stage 3_trt_tf32,4_trt_fp16,4_trt_bf16 --run-id yolo-quantization`
  </action>
  <verify>
    <automated>grep "yolo-quantization" results/results.csv | grep "yolo26l" | grep -E "3_trt_tf32|4_trt_fp16|4_trt_bf16" | grep -c "yolo26l"</automated>
  </verify>
  <done>YOLO26l standard precision benchmarks (3 entries) recorded under 'yolo-quantization'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ONNX → TensorRT | Graph is compiled into device-specific instructions; hardware-specific issues may arise. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Denial of Service | TRT Builder | mitigate | Enforce strictly 2 GB workspace limit (GEMINI.md) to prevent VRAM exhaustion on 8 GB card. |
</threat_model>

<verification>
Check `results/results.csv` for 6 new entries (yolo11l/yolo26l x Stages 3/4_fp16/4_bf16) with `run_id=yolo-quantization`.
</verification>

<success_criteria>
- All 6 engines exist in `engines/`.
- 6 benchmark entries recorded in results.csv.
- All Stages 3-4 entries show map_50_95 within 0.1% of Stage 1.
</success_criteria>

<output>
After completion, create `.planning/phases/07-yolo-family-quantization/07-02-SUMMARY.md`
</output>
